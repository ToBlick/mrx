# Whole-repo cleanup review, 2026-09-04 (findings only, nothing applied)

Twelve review passes over the worktree at 1af3c80 (li383-followups): three slices (solver core: operators, metric_lumping_laplacian, derham_sequence, solvers, preconditioners, mass, nullspace, projectors, extraction_operators; the rest of the library; scripts, tutorials, tests, slurm) by four angles (reuse, simplification, efficiency, altitude). Each agent read its slice in full and grepped the rest for callers. Line numbers are those of 1af3c80. The per-slice reports follow the summary verbatim apart from formatting; nothing here has been checked beyond what the reports state, and nothing has been changed. Tobias decides.

## Status at 3420b39 (same day, after the midpoint merge and the relaxation prune)

Checked item by item against the tree (symbols grepped; the note's line
numbers have drifted, the symbols have not).

**Done since the note.** The B-only stepper duplication (library reuse 1,
altitude 1, summary B): folded into `TimeStepper` as static branches on
`auxiliary_B_field`, `mrx/experimental/bonly_relaxation.py` deleted. The
`relax.py` options nothing runs, the `dirichlet_H` threading and the
driver's eta schedule (summary B, scripts simplification 6, altitude 5):
gone with the prune (`docs/research/handoff_2026-09-04_relax_cli_prune.md`,
"Executed"). `scripts/li383_sweep.sh` deleted. `parse_lambda` and
`dzeta_form` deleted with `--ic`. The `_PROJECTION_SPACES` lookup written
twice (simplification 9): replaced by the pair rule, and the scalar pairs
turned out to be transposed, not just duplicated (OPEN 3.12, resolved
306d174). The li383 tutorials' copies of the driver mechanics (altitude 2,
reuse 5/6, simplification 9): the tutorials session redesigned tutorials 4
and 5 on `tutorial-cell-markers` and rewired 3 and 5 to the pruned API;
judge those items after that branch lands, not from this note.

**Still valid, unchanged.** Every section-A deletion in the solver core:
the Jacobi family (the contradiction with the memory and the release
review stands: recorded deleted 2026-08-27, present in the code), the
Schur-probe and `coupled` machinery, the boundary-DoF extraction family
with `apply_bc_mass_correction` reading an attribute that no longer
exists, the dead constructor knobs, `_assemble_weighted_1d_mass` twice,
the deflation projector three times, `apply_laplacian` /
`apply_laplacian_approx`, the dense `differential_forms` evaluators,
`stellarator_map` / `extend_map_nfp`, `clebsch_form`, `update_field`,
`relaxation_loop(callback=)`, `compute_geometry_terms`,
`_bootstrap_nullspace_guesses`, `compute_divergence_norm` next to
`divergence_norm`. Every efficiency quick win: deflation applying `M` to
`x` with `mass_vs` in hand, `eps * M` with a Python zero, `eigh` per
shifted solve inside the scan, `vs_lower` and `sigma` recomputed per step,
`gap_sweeps=5` by default, `build_sequence` building all eight atoms for
plotters (`scripts/plot_mesh.py` already sidesteps it by building the
sequence and map directly: the precedent). Section B: preconditioner
selection over nine functions, the four owners of the derivative-axes
rule, the double `set_operators(compute_nullspaces(...))` install, `sp`
threaded through five reader signatures, `mu = 0.064 h^2` with the
launcher still carrying five hand-computed scales, the solve-count
experiments (velocity Leray at smoothing order 0, warm-started `J`),
`from .plotting import *`. Scripts: `quad_order_equivalence.py` and
`li383_summary.py` still exist, `slurm/regen_poincare.sh` is still its own
sbatch wrapper with the TeX path in the repo, `MRX_MAP_BATCH_SIZE_INNER`
still read in two scripts, `poincare_relax.py`'s `--from-npz` branch still
a second render path.

**Changed in kind.** `make_force_normaliser` in the driver and the
`evaluate_at_xq` reach-in in `pressure_diagnostics` are still there, but
the public surface now covers both: `dot_product_load(B, B, 0, 2, 2)` (=
`magnitude_squared_load`) and `evaluate_at_quadrature`.

**New, not in the twelve reports.** `DeRhamSequence` gained
`dot_product_load`, `scalar_product_load` and `scalar_vector_load`
(3420b39), which share one pairing helper (`_vector_load_values`,
`_scalar_load_values`); `cross_product_load_values` keeps its eight
explicit metric cases. Left on purpose so the production kernel stays
bit-identical, but it is the duplication this note flags elsewhere and
folds into the helper with a per-apply check (trajectories diverge from
round-off, so a kernel change is judged per apply, never by the end-state
residual). The `(0, 3)` / `(3, 0)` projection masses and the
assembly test that encoded their transposed convention are fixed
(306d174, 3420b39). The house style's constrained layout made every
`fig.tight_layout()` in `mrx/plotting.py` an error once a colourbar
exists; both calls removed (3420b39).

## Summary

### A. Mechanical, high confidence, no behaviour change (I would apply these)

Dead code in the solver core, each confirmed by grep to have no caller:

- The Jacobi preconditioner family: `mass_jacobi/laplacian_jacobi/schur_jacobi`, `assemble_jacobi_preconditioners`, `_diagonal_from_matvec`, `_invert_diagonal`, the five `'jacobi'` dispatch branches and the `jacobi=` kwarg (~150 lines, three bundle fields). The memory and the release review say this family was deleted 2026-08-27; the code still carries it, and `test_preconditioner_kind_dispatch.py:66` still lists `jacobi` as a live kind. With it go the Schur-probe machinery (`_build_schur_operator_apply`, `_build_schur_apply_from_saddle_preconditioner`, the unread `operator_apply` argument) and the `coupled` saddle preconditioner (`_build_coupled_saddle_preconditioner`, `precond_matvec`, `x0_lower`, the `vs_*=None` defaults).
- The boundary-DoF extraction family: `bc_extraction_op`, `boundary_extraction`, `n_boundary`, `E_bc`, `n_bc`, `bc_lift`, `load(bc=)`, projectors' `_extraction`, and `apply_bc_mass_correction`, which reads `self.m{k}`, an attribute that no longer exists (it would raise if called).
- Dead knobs: `DeRhamSequence(n_inner, r_scale, knots)`, `MetricLumpingLaplacian(bc_entry, core_tol)`, `_fd_apply_3d(eps)`, `_kron_mass_model_1d(d_raw)`, `histopolation_matrix(spans, quadrature_order)`, `trace(adaptive, rtol, atol)`, `toroid_map(kappa)`, `render_section(seed_r, cmap, denom_max)`, `plot_twin_axis`'s eight style kwargs next to the dict path, `get_2d_grids`/`plot_torus` never-passed parameters, `relaxation_loop(callback=)`, `TimeStepper.update_field`, `compute_geometry_terms`, `stellarator_map`/`extend_map_nfp` (once `_map_with_sign` is folded into `stellarator_map`), `clebsch_form`, the `lead` guard and the double clip in `clebsch_potential_form`, `SectionLimits.coerce` and the `iota_lim` legacy path, the `plotting.__all__` re-export shim, the dense per-basis-function evaluators and `Pullback`/`jacobian_determinant`/`div`/`curl`/`grad` in `differential_forms.py` (~180 lines), `_nullspace_shifted_preconditioner`, `_bootstrap_nullspace_guesses` (effect-free).
- Duplicates with one obvious owner: `_assemble_weighted_1d_mass` (twice, byte-identical); the deflation projectors (`solve_singular_cg`, twice in `solve_saddle_point_minres`, `nullspace.project_out`); `apply_laplacian`/`apply_laplacian_approx` (one function with the inner inverse swapped) and `apply_inverse_laplacian` k = 0 (= the shifted solve at eps = 0, as k = 3 already delegates); `compute_divergence_norm` vs `initial_conditions.divergence_norm`; `load_state_clebsch` vs `load_wout_clebsch` and the `.dat/.nc` dispatch pasted three times; `inv33` re-expanding `det33`; the three slope routines in `poincare.py` and the retyped `(u,v) -> (r, theta)` chart; the hand-built twin axis in `render_section`'s one branch next to `plot_twin_axis` in the other; `_coeffs_to_raw_grid` recomputing `SplineMap.raw`; `_PROJECTION_SPACES` lookup and its error twice; `_build_payload`/`_build_shifted_payload`.
- Scripts: delete `scripts/li383_sweep.sh` (passes flags `relax.py` no longer parses) and `scripts/quad_order_equivalence.py` (one-off migration check, migration landed 2026-08-28); fold or delete `scripts/li383_summary.py` (summary of the dead sweep); put `slurm/regen_poincare.sh` on `slurm/run.sh` (it hardcodes account, partition, an excluded node and TEXBIN in the repo, against `run.sh`'s rule; site values belong in `site.env`); `li383_pub_figures.py`'s four copies of the arm list and the `hsweep_figure`/`psweep_figure` pair; `poincare_relax.py`'s `--from-npz` branch re-implementing the render path; empty section headers in `test_spline_bases.py`/`test_quadrature.py`; `slurm/README.md:10` citing a test file that no longer exists; stale docstring references (`_greville_data`, `block_jacobi_laplacian`, `BlockJacobiMass`, `_elem_block_mixed`, "Jacobi diagonals, probed Schur diagonals"); `MRX_MAP_BATCH_SIZE_INNER` read once in `mrx/__init__.py` the way `precision.py` reads `MRX_DTYPE` instead of in three scripts.

Efficiency quick wins, each a few lines, each measurable against a run's `info` counts:

- Deflated solves apply `M` to `x` although `mass_vs` is precomputed: `x - (vs @ M x) @ vs` = `x - (mass_vs @ x) @ vs`. Two extra mass applies per Krylov iteration in every deflated solve, roughly half the per-iteration matvec work of the k = 3 Leray MINRES that runs every step.
- `eps * apply_mass_matrix(...)` with Python `eps = 0.0` (three sites): XLA does not fold it, one M_3 apply per MINRES iteration executed and discarded.
- Leray k = 2 discards MINRES's `sigma` and re-solves it with a second M_2 CG; `vs_lower` recomputed by a vmapped CG mass solve on every k = 3 Laplacian call for a geometry-only vector.
- `shifted_stiffness_apply` runs `eigh` on the core block per solve, twice per velocity-smoothing step inside the scan; diagonalise once at build.
- `compute_nullspaces(gap_sweeps=5)` by default runs 10 shifted saddle solves per build for a print nobody reads: the two test fixtures, the driver, three tutorials; `vacuum_convergence.py` and `analytic_vacuum.py` then compute the gap again explicitly.
- `build_sequence` builds all eight preconditioner atoms for scripts that never solve: the sections job spends ~150 s of setup at (16,32,32) against 24 s per traced field.
- The Poincaré drift check traces a data-dependent number of seeds, so both drift traces recompile for every state (~10-30 s each); the checks and probes cost ~1.4x the main trace.
- `_det_DF` sampled for both handednesses; the equilibrium file parsed and refit two to three times per run.

### B. Design-level (Tobias decides; each changes an interface, a behaviour or a solve count)

- **Preconditioner selection**: one mechanism spread over nine functions and three spec dataclasses, `valid_kinds` copied six times, "atom missing" handled five ways including silent downgrades to `'none'` and a warning-plus-identity, which the no-defensive-code rule forbids. Proposal: two bundle accessors that raise, `'auto'` = the atom unconditionally, delete the specs. An afternoon plus `test_preconditioner_kind_dispatch.py`. The atom's 1-D assembly and FD kernels live in operators.py/preconditioners.py and are imported back by the atom module through function-level imports; they belong in `metric_lumping_laplacian.py`, after which `preconditioners.py` disappears.
- **The derivative-axes rule** ("which axes are derivative axes for component c of a k-form") has four owners (mass, derham_sequence, preconditioners, metric_lumping_laplacian). One function.
- **`DeRhamSequence` install path**: `seq.set_operators(compute_nullspaces(seq, ops))` at 12 sites is a double assignment because `compute_nullspaces` already installs via `_commit`; four sequence-setup entry paths and three nullspace entry points across scripts, tutorials, tests; the `toroid` fixture calls itself the production setup but uses the sum-factorised spline-map path that only it reaches (~200 lines and an `eqx.Module` that could go if the fixture installed a closure map).
- **The B-only stepper** copies ~110 lines of `TimeStepper.relaxation_step`/`initial_state` for a two-line difference and already lags (never refreshes `H`). Two hooks on `TimeStepper`. Same decision as whether `--stepper bonly` stays in the production driver at all.
- **Driver vs library**: the force-residual normaliser (`make_force_normaliser` rebuilds `seq.magnitude_squared_load` through a private attribute), the QoI sampler, the eta schedule (two implementations, the tutorial's samples at block midpoints), the B.h5 layout (written four times with forged attributes so the plotter can read them), the seed-spec parser and `island_width` are library-grade pieces living in scripts; the three li383 tutorials are ~170-line copies of the driver's mechanics. Moving them makes the chunk/reconnect/restart loop testable within the lean-suite rule.
- **Equilibrium readers**: `sp` threaded as an optional through five gvec signatures; one reader returning a state that carries its profile splines; `geometry_nfp` from the state.
- **`relax.py` options nothing runs**: `--eta-schedule constant|linear`, `--method gradient`, `--dt-mode fixed`, `--ic dzeta`, `--presmooth*` (kept for the deleted h5 route), `--stepper bonly`; `dirichlet_H` is never true anywhere yet threaded through ~12 sites. Which experiments are closed is the question.
- **`mu = 0.064 h^2`** has six owners (three tutorials, the test, the docs, five hand-computed scales in the launcher); a default in the stepper or driver.
- **Solve-count changes with measurable effect on the step** (each needs a run to confirm): skip the velocity Leray projection at smoothing order 0 (the velocity is already in `ker div` to solver tolerance and the projection's CG iterates on a round-off rhs; plausibly 15-25% of the step); warm-start `J` and `sigma` by carrying them in `State` (three cold mass solves per step, 10-20%); the atomic `segment_sum` in the sum-factorised mass kernel; `E`/`E^T` as full gathers where a static slice covers the bulk; geometry arrays embedded as HLO constants in every executable (check with `run.lower(...).as_text()`); the second de Boor pass for derivative bases in the tracer RHS.
- **`from .plotting import *` in `mrx/__init__.py`** pulls matplotlib into every `import mrx`, including headless GPU jobs.

Contradiction worth a look: the memory and release review record the Jacobi family as deleted on 2026-08-27; the code, the bundle fields and the dispatch test still carry it.

---

## Solver core: reuse

1. **Schur-operator apply == `apply_laplacian_approx`; the Jacobi build probes it twice.** (high) `mrx/operators.py:1102-1151` (`_build_schur_operator_apply` via `_build_schur_apply_from_saddle_preconditioner`, eps=0, inner = `_mass_metric_lumping_for(k-1)`) computes `S_k + D B D^T` with `B` the metric-lumped mass atom; `apply_laplacian_approx` (`operators.py:1846-1889`) computes exactly that (its `kind='auto'` resolves to the same atom). In `assemble_jacobi_preconditioners` (`operators.py:1015-1021`) `lap[key]` and `schur[key]` are the same diagonal probed twice, O(n_k) applies each, per (k, BC) with k >= 1. Call `apply_laplacian_approx` (or one shared builder) and drop `schur_jacobi` for k >= 1.
2. **`apply_laplacian` / `apply_laplacian_approx` are one function with the inner inverse swapped.** (high) `operators.py:1805-1843` and `1846-1889`: two 40-line `match k` blocks identical except `apply_inverse_mass_matrix` vs `apply_mass_matrix_preconditioner`. One `_laplacian_apply(seq, v, k, dirichlet, minv)` removes about 40 lines and a second copy of the k = 1/2/3 structure.
3. **Deflation projectors written three times in solvers.py.** (high) `mrx/solvers.py:152-167` (`solve_singular_cg`) and `:444-464` (`solve_saddle_point_minres`, upper and lower) each build `(project_primal, project_dual)` from `(vs, mass_matvec)` with identical 4-line formulas and the identical empty-`vs` branch; `mrx/nullspace.py:663-668` (`project_out`) is a fourth copy of `project_primal`. One `deflation_projectors(vs, mass_matvec)` serves all.
4. **`_assemble_weighted_1d_mass` defined twice, byte-identical.** (high) `mrx/operators.py:85` and `mrx/preconditioners.py:131`; `metric_lumping_laplacian.py:74` imports the operators one, `preconditioners.py:189` uses its own.
5. **"Which axes are derivative axes for component c of a k-form" encoded four times.** (high) `metric_lumping_laplacian.py:565` (`derivative_axes`), `preconditioners.py:135` (`_metric_lumping_diff_flags`, docstring admits it "mirrors" mass.py), `mass.py:89-100` (`_component_axis_bases_k1/k2` + the k = 0/3 lambdas in `_form_bases`), `derham_sequence.py:498-536` (`_form_comp_info`). Also `_axis_bases` (`metric_lumping_laplacian.py:145`) returns the `(primal, deriv, quad_w)` triple that `preconditioners.py:181-183` inlines again. One rule + one table selector.
6. **Preconditioner-spec coercers: four near-identical functions.** (medium) `operators.py:173` (`_coerce_diffusion_preconditioner_spec`), `:909` (`_coerce_mass_preconditioner_spec`), `:1054` (`_resolve_mass_preconditioner`), `:1154` (`_coerce_scalar_hodge_preconditioner`). `_materialize_default_mass_preconditioner` (`:99`) is a pure alias. The `valid_kinds` check + `'none'`/`allow_none` branch is repeated verbatim at `:1065-1081`, `:1215-1223`, `:1257-1265`.
7. **Natural-BC face average computed twice; one branch dead.** (medium) `metric_lumping_laplacian.py:285-286` (`_face_alpha.fm`) and `:364-367` (`_boundary_entry_direct`, `scalar is None` branch) are the same einsum; the only caller (`:494`) always passes `scalar=`. Delete the branch and the `weight_field` argument.
8. **Payload packing duplicated inside `MetricLumpingLaplacian`.** (medium) `metric_lumping_laplacian.py:978-1003` (`_build_payload`) and `:1012-1038` (`_build_shifted_payload`): identical `_LumpBlock` loop and `_output_permutation` call, differing only in which `alpha` is packed. `_LumpPayload`/`_MassPayload` (`:697`, `:1078`) carry the same five fields.
9. **`MatrixFreeExtraction.from_coo` bypassed in its own module.** (medium) `extraction_operators.py:37` exists; `:426-438` (`build_extraction`) and `:495-501` (`bc_extraction_op`) redo the conversions and construct directly. `_StencilTriplets.emit` (`operators.py:488`) and `PolarExtractionOperator._append_triplets` (`extraction_operators.py:164`) are two zero-dropping COO collectors; `_stencil_grid` (`operators.py:475`) is spelled inline at `extraction_operators.py:230`.
10. **Polar-core discriminator duplicated.** (medium) `projectors.py:209-212` (`_conforming_restriction`) recomputes `counts = bincount(e.rows) > 1`, docstring cites the stale name `block_jacobi_laplacian.core_rows`; the helper is `metric_lumping_laplacian.py:594` (`core_rows`).
11. **`get_saddle_point_nullspaces._lower` is a weak derivative.** (medium) `nullspace.py:112-119` computes `M_{k-1}^{-1} D_{k-1}^T v`, i.e. `seq.apply_weak_curl` / `-apply_weak_div` / `-apply_weak_grad` (`derham_sequence.py:577-598`); sign per k to handle.
12. **Small aliases / repeated idioms.** (medium) `nullspace.py:65` `_dof_count` is `seq.n` (5 sites); `derham_sequence.py:418` `_require_operators` vs `operators.py:78` `_require_bundle`; `seq.E(k, True) if d else seq.E(k)` (`projectors.py:451,548,563,573`) and the same for `seq.n` (`operators.py:590-591,694-695`, `derham_sequence.py:1056`) are `seq.E(k, d)` / `seq.n(k, d)`; every solve forwarder in `derham_sequence.py:609-810` resolves `tol/maxiter` although the `op.*` functions do the same on entry (`operators.py:868, 1488, 1534, 1593, 1767`); `derham_sequence.py:493` `apply_bc_mass_correction` reads `getattr(self, f'm{k}')`, an attribute nothing sets: dead re-implementation of `apply_mass_matrix`.

Clean: `mrx/mass.py` beyond item 5.
## Solver core: simplification

1. **Jacobi preconditioner family is dead.** (high) `operators.py:56-62, 960-1023, 1075-1077, 1224-1229, 1266-1273, 1367-1368, 1663-1667`; `derham_sequence.py:350, 389-390, 394-398`. No caller passes `jacobi=True` or `kind='jacobi'` (the only hit, `test/test_preconditioner_kind_dispatch.py:66`, is a source-scanning lint listing it as accepted). Delete `mass_jacobi/laplacian_jacobi/schur_jacobi`, `assemble_jacobi_preconditioners`, `_jacobi_entry`, `_mass_diaginv`, `_laplacian_diaginv`, `_schur_diaginv`, `_diagonal_from_matvec`, `_invert_diagonal`, the five `'jacobi'` branches and the `jacobi=` kwarg: about 150 lines and three bundle fields, and a contradiction with the documented state (memory: jacobi family deleted 2026-08-27).
2. **Schur-operator machinery has no live consumer.** (high) `operators.py:1102-1151, 1061-1082, 1669-1685`. `_build_operator_preconditioner_apply` takes `operator_apply` and never reads it; the only real user is the Jacobi probe. With 1 gone, `_build_schur_operator_apply` and `_build_schur_apply_from_saddle_preconditioner` go too, and `_build_operator_preconditioner_apply` collapses into `_build_mass_preconditioner_apply`.
3. **`coupled` saddle preconditioner is dead.** (high) `operators.py:119, 149, 1287-1306, 1686-1697`; `preconditioners.py:60`; `solvers.py:385, 411-413, 498-500`. Nothing sets `coupled=True`. Remove `_build_coupled_saddle_preconditioner`, the field, `precond_matvec` in `solve_saddle_point_minres`; `x0_lower` (388) and the `vs_*=None` list defaults (432-435) are likewise unused.
4. **Boundary-DoF extraction family is dead.** (high) `derham_sequence.py:226-233, 312-322, 450-496`; `extraction_operators.py:478-501`; `projectors.py:243-250, 258, 299`. No caller uses `bc=True`, `E_bc`, `n_bc`, `bc_lift` or `apply_bc_mass_correction` (which reads `self.m{k}`, gone). Delete `bc_extraction_op`, `boundary_extraction`, `n_boundary`, the four methods, `load(bc=)` and projectors' `_extraction`.
5. **Dead constructor/builder knobs.** (high) `derham_sequence.py:128-130, 169-170, 194` (`n_inner` never read; `r_scale`, `knots` never passed; the `knots` docstring is about multigrid coarse levels); `metric_lumping_laplacian.py:378, 542, 899-900, 1122` (`bc_entry`, `core_tol` never passed; `bc_entry=False` redundant with `dirichlet=True`); `operators.py:246` (`_fd_apply_3d(eps=)` never passed); `preconditioners.py:154` (`d_raw=None` never passed; docstring cites a non-existent `build_mass_metric_lumping_factors`); `operators.py:1029` (`dirichlet_variants` vs `dirichlets` across the two assemblers).
6. **`'auto'` silently degrades instead of raising.** (high) `operators.py:142, 166-170, 1370-1376`: `_materialize_default_saddle_preconditioner` drops to `outer='none'`, `_materialize_default_scalar_hodge_preconditioner` to `'none'` when the atom is missing, `apply_laplacian_preconditioner(kind='auto')` warns and returns the identity. Fallbacks the no-defensive-code rule forbids; the `eps != 0 -> 'none'` rule at 166 contradicts the comment at 1275 and the only shifted caller (`nullspace.py:165-182`) overrides it. Make `'auto'` mean `metric_lumping` unconditionally, raise when absent, delete `_nullspace_shifted_preconditioner`.
7. **`p = 1` stand-in is a leftover.** (medium) `metric_lumping_laplacian.py:159-233, 405, 466-471`: no sequence in the repo runs p = 1; 75 lines plus a duplicated comment block (216-231).
8. **`apply_laplacian` / `apply_laplacian_approx` are 3x copy-paste.** (high) `operators.py:1813-1843, 1856-1889`: cases 1, 2, 3 differ only in `k-1`, and `apply_stiffness(k=3)` already returns zeros, so each `match` is two lines. `apply_inverse_laplacian` k = 0 (1537-1567) duplicates `apply_inverse_shifted_laplacian` k = 0 with eps = 0 instead of delegating as k = 3 does.
9. **Duplicated helpers.** (high) `_assemble_weighted_1d_mass` twice; the derivative-axis rule three times; `_PROJECTION_SPACES` lookup and its error message twice (`operators.py:750-760, 781-793`); `_build_payload`/`_build_shifted_payload` (`metric_lumping_laplacian.py:963-1038`); `_oneform_pullback`/`_twoform_pullback` (`projectors.py:423-492`) and the four `e = ...; exact = _matching_discrete_dofs(...)` preambles (449-576); the k = 1 and k = 2 surgery loops in `extraction_operators.py:273-343, 360-402`.
10. **Derivable/redundant atom state.** (medium) `metric_lumping_laplacian.py:507-510, 901-908, 948-959`: `component_factors` always returns `alpha=(1,1,1)`, so `_LumpBlock.alpha` is a constant leaf for the Laplacian payload and the pytree note at 663 is stale; `probe_rows` duplicates `core`; `k, dirichlet, bc_scale, shapes, bulk, n_ext, blocks` stored and never read outside `__init__`; `_boundary_entry_direct` (341-377) always receives `scalar`; `_face_alpha` computed before the Dirichlet early return discards it.
11. **Trivial forwarders and dead checks.** (high) `operators.py:23-32, 72` (lazy imports of `mrx.nullspace`: no cycle exists); `763-778` (`extraction`, `_mass_extraction`, `_derivative_extraction` wrap `seq.E`); `823, 901` (`g_sp is None` never true); `99-114, 173-181` (functions that `del` every argument); `1054-1058`; `1178-1191` (`valid_outer_kinds` twice); `nullspace.py:65` (`_dof_count`); `derham_sequence.py:700` (`apply_mass_plus_eps_laplace_matrix`, unused); `projectors.py:41-42` (unused type aliases); `extraction_operators.py:53-55, 82-86` (unused `dtype`, duplicate `__call__`/`__matmul__`, also on `_MatrixFreeIncidence`).
12. **Effect-free step.** (medium) `nullspace.py:141-162, 558-564`: `_bootstrap_nullspace_guesses` stores guesses "so fallback lookups see them", but the shifted solve passes zero deflation for eps > 0 and `apply_laplacian` never deflates, so nothing reads them before `find_nullspace_vectors` overwrites the slot.
13. **Stale docstring references.** (high) `projectors.py:14, 85, 192, 201, 362` (`_greville_data`, `block_jacobi_laplacian`, `BlockJacobiMass`); `mass.py:152, 166` (`_elem_block_mixed`); `operators.py:36-45` ("Jacobi diagonals, probed Schur diagonals").

Clean: `solvers.py` beyond 3; `mass.py` beyond 9/13. Note: `extraction_operators.py` has 14 `mode="clip"` gathers that silently clip out-of-range indices; the default (`raise`) is simpler and honest.
## Solver core: efficiency

1. **Deflation projections apply the full mass operator when the product is already precomputed.** (high) `solvers.py:161-163, 450-451, 461-462`: `project_primal(x) = x - (vs @ M x) @ vs`; with `M` symmetric this is `x - (mass_vs @ x) @ vs`, and `mass_vs` is already formed (159, 449, 460). Cost: 2 extra mass applies per Krylov iteration in every deflated solve. k = 3 Leray MINRES (every step): 2 M_3 + 2 M_2 extra against 2 M_3 + 1 M_2 needed, roughly half the per-iteration matvec work; k = 0 free pressure solve 30-50%.
2. **`eps * apply_mass_matrix(...)` with Python `eps = 0.0`.** (high) `operators.py:1700-1702` (also 1614-1616, 1120-1121): XLA does not fold float `x*0`, so the k = 3 Leray MINRES executes and discards one M_3 apply per iteration. Skip the term when `eps` is a Python zero.
3. **`shifted_stiffness_apply` runs `jnp.linalg.eigh` on the core block per solve.** (high) `metric_lumping_laplacian.py:1056-1057`: two per velocity-smoothing step (k and k-1), inside the scan. Diagonalise `(mass_core, stiffness_core)` once at build; `(M + eps S)^{-1} = V diag(1/(1+eps mu)) V^T`, two small matmuls, eps may stay traced. Cost: cuSOLVER syevd at n_core = 160-320, several ms per solve.
4. **`vs_lower = M_2^{-1} D_2^T h` recomputed by a vmapped CG mass solve on every k = 3 Laplacian call** (every Leray projection) for a geometry-only vector that is ~0. (high per step, medium cost) `operators.py:1630` + `nullspace.py:100-122`. Compute once in `compute_nullspaces`, store on the bundle. Cost: one M_2 CG solve (10-20 M_2 + atom applies) per step.
5. **Leray k = 2 discards MINRES's `sigma` and recomputes it.** (high) `derham_sequence.py:1049-1052`: the lower block gives `sigma = M_2^{-1} D_2^T q`, exactly what `-apply_weak_grad(q)` re-solves with a second M_2 CG. Return `sigma` from `apply_inverse_shifted_laplacian`. Cost: one M_2 solve per step.
6. **`_sumfact_kernel` assembles with an atomic `segment_sum` over n (p+1)^3 entries.** (medium) `mass.py:377`: 64x n at p = 3, ~25M adds per k = 1, 2 apply at (32,64,64), the largest single memory op in the apply and non-deterministic. A fixed padded gather plan `(n, (p+1)^3)` plus a sum over the last axis.
7. **`_fd_apply_3d` eps == 0 recomputes `max|denom|`, the null mask and two `where`s per apply** although `denom` depends only on the payload. (medium) `operators.py:254-261`. Store the masked `inv_denom` as a `_LumpBlock` leaf. Cost: 3 block reductions + masks per atom apply (apply ~70-90 us).
8. **Every E / E^T apply is a full-length gather + `segment_sum`** though E is a pure selector on all but the ~5 nz polar rows; 6-8 such applies per Krylov iteration. (medium) `extraction_operators.py:67-80`. Static slice for the bulk + tiny gather for the core (the `offset` idiom `_block_input` uses).
9. **`r0 = b - A(x0)` with `x0 = 0`, and MINRES forms `M(b)` separately from `y0 = M(r0)`.** (high, low impact) `solvers.py:71, 252, 258`. One operator + one preconditioner apply per solve, 5-10% of a 10-20 iteration mass solve.
10. **Build-time waste** (one-off per geometry; matters in an outer optimisation loop). (medium) `metric_lumping_laplacian.py:625-627` `_probe_rows` runs `apply_laplacian_approx` op-by-op (~30 dispatches per column, 8k columns, seconds of dispatch): jit once per probe or batch 16-wide as `_diagonal_from_matvec` does; `:955-958` and `:1147` probe M_k on the core rows twice; `:438, :513`, `preconditioners.py:178` recompute `stiff_prof` (identical for every k, c, BC), `component_diagonal` and `build_mass_diagonal` per (k, c, BC), ~70 redundant full-grid reductions; `derham_sequence.py:349` builds all 8 (k, BC) atoms whether or not the driver's solves use them; `mass.py:331` the four projection applies each hold a full-N_q weight array.
11. **`quad.w / jacobian_j` recomputed per `cross_product_load_values` / `magnitude_squared_load` call.** (high, low impact) `derham_sequence.py:943, 950, 966, 983`. Hoist to the geometry.

Clean: `preconditioners.py` (build only), `projectors.py` (IC time), `nullspace.py` beyond 4. No per-step host syncs or recompilation triggers found.
## Solver core: altitude

1. **Preconditioner selection is one mechanism spread over nine functions and three spec dataclasses.** (high, now) `mrx/operators.py` 99-187, 909-917, 1054-1099, 1154-1202, 1205-1284, 1347-1383, 1638-1697; `mrx/preconditioners.py` 26-61. `valid_kinds = ('none','jacobi','metric_lumping')` copied six times; "is the atom on the bundle" checked five times with four different outcomes for the same missing atom (silent `'none'` at 142 and 168-170, warning + identity at 1370-1375, `ValueError` at 1244, 1277, 1408, 1654). `SchurPreconditionerSpec.outer` defaults to `'none'` with a comment saying the real default lives elsewhere, so every construction site passes `outer=` by convention. `_materialize_default_mass_preconditioner` is `del args; return default`; `_build_operator_preconditioner_apply` takes an `operator_apply` it never reads (1061-1082); `coupled=True` / `_build_coupled_saddle_preconditioner` (1287-1306) has no caller. Recommend two bundle accessors that raise, `mass_atom(k, d)` / `laplacian_atom(k, d)`, `'auto'` = the atom unconditionally, delete the spec dataclasses or strip their defaults. Cost: an afternoon plus `test_preconditioner_kind_dispatch.py`.
2. **Derivative-axes rule has four owners** (high, now): same as reuse item 5; one function returning the derivative-axis tuple per (k, c).
3. **Atom internals live in operators.py / preconditioners.py and are imported back by the atom module.** (medium-high, now) `operators.py` 85-96, 190-275 (`_dense_incidence_1d`, `_fd_*`); `preconditioners.py` 108-202 (`_simultaneous_diagonalize_pair`, `_kron_mass_model_1d`). Only consumer is `metric_lumping_laplacian.py` (73-81), which operators.py lazily imports at 1038/1336 while it imports operators at 643/953/1123: an import cycle papered over with function-level imports. Move all 1-D assembly and FD kernels into `metric_lumping_laplacian.py`; `preconditioners.py` disappears with item 1.
4. **Dead or orphaned surface on `DeRhamSequence`.** (high, now) `apply_bc_mass_correction` (470-496) reads `self.m{k}`, which no longer exists; `bc_lift` (450) no caller; `n_inner` (130, 169, 194) stored, never read; `polar` (127, 184-189, 213) has one legal value; `get_operators`/`set_operators` (409-416) wrap a public attribute, and since `compute_nullspaces` already assigns `seq.operators` via `_commit` (nullspace.py 133-138), `seq.set_operators(compute_nullspaces(seq, ops))` at 12 script/test sites is a double assignment. Pick one owner for the install.
5. **Duplicated solve bodies.** (high, now, small) `apply_inverse_laplacian` k = 0 (1537-1567) is `apply_inverse_shifted_laplacian` at eps = 0 (1596-1628), and k = 3 already delegates that way; `apply_laplacian`/`apply_laplacian_approx` (reuse item 2); `tol/maxiter` defaulting in every forwarder and again in every operators function.
6. **Script re-implements a library method.** (high, now) `scripts/relax.py` 295-320 (`make_force_normaliser`) rebuilds `seq.magnitude_squared_load` (`derham_sequence.py` 973-985) by hand through `seq._form_comp_info` + `evaluate_at_xq`; `mrx/relaxation.py` 190-194 does the same reach-in while calling `seq.evaluate_at_quadrature` two lines later (201). Replace both with the public methods (the 3-form's `/J` convention to note).
7. **Two owners of "polar core rows" and of the dense Gram-block inverse.** (medium, later) `metric_lumping_laplacian.py` 594-609 (`core_rows`) and `projectors.py` 209-226 (`_conforming_restriction`), which rebuilds a scipy Gram + connected components on every `interpolate` (its own TODO at 205). The extraction or sequence init should expose `core_rows` and the per-block `(E E^T)^{-1}` once.
8. **Deflation projection twice in solvers.py with two input conventions** (medium, later): reuse item 3.
9. **Two null-eigenvalue cut-offs for one rule.** (medium, later) `operators.py` 254-261 (`_fd_apply_3d`, `sqrt_eps(6.7e-3)` relative mask) vs `metric_lumping_laplacian.py` 85, 631-638 (`CORE_TOL = eps(4096)`). One threshold in one place; general kernel handling is deflation in the solver.
10. **Jacobi family carried through production dispatch.** (medium, later, folds into 1) `operators.py` 56-62, 960-1023, jacobi branches in every dispatcher, `_build_schur_apply_from_saddle_preconditioner` (1127-1151) existing mainly to probe it. If it is the diagnostic it looks like, it should be a separate diagnostic module producing a callable, not three bundle fields and a kind string in every solve.
11. **Two one-hot probing routines.** (low, later) `operators.py` 938-952 (`_diagonal_from_matvec`, batched 16) and `metric_lumping_laplacian.py` 612-628 (`_probe_rows`, one apply per row).

Clean: `mrx/mass.py`; `mrx/extraction_operators.py` (only a stale module docstring); `mrx/nullspace.py` apart from `_commit`'s side effect (item 4).
## Library rest: reuse

1. **`BOnlyTimeStepper.relaxation_step` is an 80-line copy of `TimeStepper.relaxation_step`** (`bonly_relaxation.py:55-136` vs `relaxation.py:548-705`): only the force (58-63) and `u x B` for `u x H` (85) differ; same for `initial_state_bonly` (139-167) vs `initial_state` (708-742) and `compute_force_bonly:42-43` vs `compute_force:71-74`. Two hooks on `TimeStepper`. (high)
2. **`relaxation.compute_divergence_norm` (52-58) duplicates `initial_conditions.divergence_norm` (295-299).** `scripts/relax.py:399-426` uses one, `chunk_runner:779` the other. Keep one. (high)
3. **`relaxation.py:190-193` re-implements `seq.evaluate_at_quadrature` for k = 0 and 3** through `seq._form_comp_info` + `evaluate_at_xq`, while calling the public method 8 lines later for k = 1, 2. (high)
4. **`initial_conditions.py:283-287` `B_phys = dF @ omega / det(dF)` is `Pushforward(omega_ref, seq.map, 2)`** (`differential_forms.py:300-331`); `tutorials/qa_vacuum_field.py:77` uses it. Piola formula in two places, here with `jnp.linalg.det` instead of `det33`. (high)
5. **`gvec._map_with_sign` (171-178) duplicates `mappings.stellarator_map` (125-146)**; `stellarator_map` fixes `sign=-1` and has no callers; `_map_with_sign` is imported by `scripts/map_projection_study.py:28`. Give `stellarator_map` the `sign` argument, drop `flip_zeta`, delete `_map_with_sign`. `gvec.py:181-189` `_det_DF` re-derives what `jacobian_determinant` (`differential_forms.py:414-416`, no callers) provides. (high)
6. **`vmec.load_wout_clebsch` (236-247) is `gvec.load_state_clebsch` (357-367) line for line** (only `profile_spline`); the `.dat/.nc/raise` dispatch and its error string copied at `gvec.py:314-320`, `391-397`, `geometry.py:333-340` (and `map_projection_study.py:81` without the guard). (high)
7. **`poincare.py:337-343, 382-389, 421-429`: unwrapped angle + least-squares slope written three times**; `trace_and_classify:607-612` calls all three on the same `ys, centre`, so the `(n_seeds, n_saves)` unwrap runs three times per trace. The `(u,v) -> (r, theta % 1)` inverse chart of `_uv_to_logical:178-181` is retyped at 278, 438-439, 541-542, 555-556, 668-669. (high)
8. **`plotting.py:654-682` builds the iota/p twin axis by hand** (`twinx`, coloured labels, `tick_params`), the job of `plot_twin_axis` (255-316), which the other branch of the same function uses at 717-722. The resonant-tick loop (684-687 vs 734-737) and the ylim/xlim/grid block (688-692 vs 738-742) are duplicated across the two branches. (medium-high)
9. **`geometry.py:178-196` `_coeffs_to_raw_grid` recomputes `SplineMap.raw`** (`mappings.py:36-37`); `compute_geometry_terms:72-88` no callers. (high, low cost)
10. **`gvec.py:239-241, 259-262` composite Gauss rescale is `composite_quad` (`quadrature.py:59-77`); `projectors.py:91-105` `_span_quadrature` a third copy.** Caveat: `composite_quad` returns jnp at working precision, the gvec fits are float64 numpy on purpose; the fix is a numpy core that `composite_quad` wraps. `vmec.py:135` retypes the Greville formula of `SplineBasis.greville_points` (`spline_bases.py:230-247`). (medium)
11. **`differential_forms.inv33` (402-411) re-expands the determinant instead of `adj33(mat) / det33(mat)`.** (high, trivial)

Clean: `plotstyle.py`, `precision.py`, `__init__.py`, `quadrature.py`, `spline_bases.py` (the `_p_spline` vs `_nonzero_bsplines` pair is a documented reference/production split), `mappings.py` bar 5, `differential_forms.py` bar 11.
## Library rest: simplification

1. **`bonly_relaxation.py:55-167`: ~110-line copies of `relaxation.py:548-742`.** (high) Two hooks on `TimeStepper`, the subclass overrides ~10 lines.
2. **Sum-factorised spline-map path reachable only from `test/conftest.py:83`.** (medium) `mappings.py:11-49 SplineMap` (`with_coefficients` no caller; `extraction_T` is `extraction.T`), `geometry.py:31-47 grad_1d`, `150-171 from_spline_map`, `178-264` (`spline_map_F_DF_at_quad` returns `F_q`, discarded), `271-297 greville_interpolate_map`, `derham_sequence.py:431-448 build_spline_map/geometry_from_spline_map/set_spline_map`. Production maps are `DiscreteFunction` closures through `set_map`. The fixture could interpolate per component and install a closure via `set_map`; ~200 lines and an `eqx.Module` go.
3. **`geometry.compute_geometry_terms` (72-88): no caller.** (high)
4. **Dense per-basis-function evaluators with no caller.** (high) `differential_forms.py:121-243` (`_vector_index`, `_ravel_index`, `_unravel_index`, `evaluate`, `__call__`, `vecs`), `spline_bases.py:371-380 TensorBasis.evaluate/__call__` (+ `.shape`); docstring cites a `test_sequence` that does not exist (`test_spline_bases.py:104` TensorBasis section is an empty header). Also dead: `Pullback` (335-368), `jacobian_determinant/div/curl/grad` (414-442); `DiscreteFunction.dof`/`.E` stored, never read. ~180 lines.
5. **`mappings.py` leftovers.** (high) `stellarator_map` (incl. `flip_zeta`) and `extend_map_nfp` (149-176): no callers; `rotating_ellipse_map:71-78` `if nfp > 0` branches dead after the raise at 61; `toroid_map(kappa)` never passed.
6. **`poincare.py`.** (high) `trace(adaptive=, rtol=, atol=)` 208-256 never passed: research knob, delete the PID branch. The three slope routines (320-433) re-derive centre, unwrapped angle, zeta, slope; `center=None` fallbacks never taken. `BzetaParameterisationError` attributes never read. Line 674 stray comment from a deleted dict.
7. **`plotting.render_section` (408-759) and `plotstyle.SectionLimits.coerce` (107-118).** (high) `seed_r` consumed only when `profile_x is None` (both callers pass it): dead positional; `cmap`, `denom_max` never passed; `iota_lim` + `limits` through `coerce`, self-described "legacy": nothing constructs a `SectionLimits`, the `isinstance` branch is dead. One `limits` argument, no `coerce`. `iota_scatter=None` fallback with its second ribbon label (632-634) exists for pre-2026-09 archives: back-compat. `__all__`/comment 30-35: no such caller.
8. **Dead style parameters, `plotting.py`.** (high) `plot_twin_axis` 254-316: eight explicit style params AND `left/right_plot_kwargs`; `get_2d_grids` 38-97: `tol2/tol3, x_min..z_max, invert_x/invert_y` never passed; `plot_torus`: `labelsize/ticksize/noaxes` never passed. 25 parameters across three signatures.
9. **`gvec.py`/`vmec.py`/`geometry.py` dispatch and reader duplication.** (high; `sign=` medium) `load_state_clebsch` = `load_wout_clebsch`; three `.dat/.nc` dispatchers with the same error string; `read_equilibrium` the only dispatcher, `geometry_nfp = read_equilibrium(path)["nfp"]` (then `vmec.read_nfp` goes). `build_gvec_map` 338-352 builds `R_fn/Z_fn` StateFields and returns `R_h/Z_h/R_fn/Z_fn` in `info`: no consumer; `sign=` never passed. `read_state["hmap"]`, `_state_from_raw`'s `ns/mnmax/signgs/version` never read.
10. **`relaxation.py`.** (high; `dirichlet_H` medium) `update_field` 541-546 no caller, 25-name `Literal`; `compute_divergence_norm` duplicate; 75-76 commented-out code; 614-621 a comment about a former comment; 856/858 `+ 1e-30` guards in a print; `relaxation_loop(callback=)` never passed; `dirichlet_H` never `True` anywhere yet threaded through `compute_force`, `weak_pressure`, `TimeStepper`, `bonly`, `initial_state` (~12 sites).
11. **`initial_conditions.py`.** (high) `clebsch_form` 126-150: only a comment in `relax.py:388` mentions it; the potential route replaced it. `clebsch_potential_form` 186-190 `lead` guard: both loaders give `rho = linspace(0, 1, 401)`, so dead; 214-215 double `jnp.clip` on `r` no-ops.
12. **`spline_bases.histopolation_matrix(spans=, quadrature_order=)` 498-531 never passed.** (high) Hardcode.
13. **`__init__.py:7 from .plotting import *` pulls matplotlib into every `import mrx`, including headless GPU jobs.** (medium) Lazy import.

Clean: `quadrature.py`, `precision.py`, `plotstyle.py` bar `coerce`.
## Library rest: efficiency

Per-step numbers against the 0.57 s step at (16,32,32) p = 2; CG stops on `||r||_M < tol ||b||_M` (`solvers.py:118`), which matters for 1 and 2.

1. **Velocity Leray projection solves for round-off.** (medium; verify with the solve's `info`) `relaxation.py:592` (also `bonly_relaxation.py`): `u` from the two-loop recursion is a linear combination of `F`, `F_prev` and `dt u_prev`, all already Leray-projected; with smoothing order 0 `div u` is at solver tolerance before the projection runs. The projection then costs one k = 3 Laplacian solve plus one cold k = 2 mass solve whose rhs is the divergence residual, and because CG's tolerance is relative to that rhs the solve iterates a full count (in float32 possibly to `maxiter`). Skip when the smoothing order is 0 (with smoothing on, the docstring's own argument at l.646 says `(M + eps L)^-1` preserves `ker div`). Plausibly 15-25% of the step.
2. **Three cold-started mass solves per step.** (high mechanism, medium size) `relaxation.py:77` (J) and `apply_weak_grad` at `derham_sequence.py:1034` (sigma, once in the force Leray and once in 1): `apply_weak_curl`/`apply_weak_grad` take no `guess`, while `H`, `JxH`, `E`, `p`, `p_v` are warm-started. J and sigma change by O(dt) per step; a warm start converges in a handful of iterations instead of 20-40. Carry `J` (already returned by `compute_force`, discarded at l.558) and `sigma` in `State`. 3 of ~8 mass-level solves per step, 10-20%.
3. **Poincaré checks and probes cost more than the section itself.** (medium launch-bound assumption, high recompile) `poincare.py:535-546, 633`: each Tsit5 step is ~300 tiny kernels over <= 100 lanes, so wall time is per step: main trace 6400 steps; drift check 6144 steps on 8 lanes; two axis probes 3072 steps on 2 lanes: the checks are ~1.4x the main trace, and `walltime` reports only the main trace. `seeds[idx]` at l.633 has a data-dependent length (`regular[::len//8]` gives 8-15 seeds), so both drift traces recompile for every state (~10-30 s each). Fix `idx` to exactly `drift_seeds` entries; cut `drift_periods` and `probe_periods` to ~16.
4. **Derivative bases evaluated by a second de Boor pass.** (medium) `spline_bases.py:450-456` via `differential_forms.py:577-584`: a k = 2 evaluation does 6 `_nonzero_bsplines` passes per point (Lambda and dLambda per axis); dLambda's values are the level-(p-1) row of Lambda's own recursion. The RHS of every tracer step and every `DiscreteFunction` vmap. 25-35% of RHS kernels per trace step.
5. **Geometry arrays embedded as HLO constants in every executable.** (medium; check `run.lower(...).as_text()` size first) `relaxation.py:785, :818`, `scripts/relax.py:417-500`: `run`, `get_helicity`, `pressure_probe`, `smooth`, `reconnect_fn`, `normaliser` close over `seq`; on JAX 0.9.0 `jax_use_simplified_jaxpr_constants` defaults False, so each executable carries its own copy of the metric arrays (~70 MB at this mesh, ~160 MB at p = 3). Try the config flag in `precision.py`, or pass `SequenceGeometry` as a jit argument.
6. **`_det_DF` sampled for both handednesses.** (high, one-off 1-3 s) `gvec.py:346-352`: flipping Y negates det DF exactly; one pass, `s = sign(d)`.
7. **Equilibrium file parsed/refit two to three times per run.** (high, one-off seconds) `gvec.py:336, :361/:672`, `geometry.py:335`: `build_gvec_map`, `load_clebsch`, `geometry_nfp` each re-read; for a wout three `_fit_block` refits. `lru_cache` on `read_equilibrium(path)`; `design_matrix(..., nu=o)` for the derivative rows.
8. **Pushforward/pullback round trip with two `jacfwd` passes.** (medium, one-off 2-4 s) `initial_conditions.py:283-290`: `DF omega/J` then `adj(DF)` inside `load(frame='phys')` is the identity on omega; the only content is the `g omega/J` weighting, available from the resident metric.

Below threshold: `Fu` re-applies `M_2` (`relaxation.py:780`; equals the linesearch numerator); `H_jk` evaluated twice; the per-step `div` trace of an exactly conserved quantity; `pressure_diagnostics` recomputing wall DF per call.

Clean: `plotstyle.py`, `precision.py`, `quadrature.py`, `mappings.py`, `vmec.py` beyond 7, `geometry.py` beyond 7, `plotting.py`, `spline_bases.py` tables.
## Library rest: altitude

1. **`mrx/experimental/bonly_relaxation.py:55-167` copies the production step body.** (high, now) `BOnlyTimeStepper.relaxation_step` is about 80 lines of `TimeStepper.relaxation_step` (`relaxation.py:548-705`) with two lines changed (how `F` and `E_dual` are formed); `initial_state_bonly` copies `initial_state`; `scripts/relax.py:342-345` import-renames three symbols. Fix: two hooks on `TimeStepper`, `force(state) -> (F, p, H, JxH)` and `electric_field(u_jk, state) -> E_dual`, called from the one step; B-only overrides the two (about 15 lines). The copy already lags (the B-only step never refreshes `H`).
2. **`mrx/gvec.py`: `sp` threaded as an optional through five signatures.** (high, now) `block_knots` (108) branches on `"T" in block`; `sp` passed to `StateField`, `series_spline_dofs`, `_radial_coefficients`, `build_gvec_map`, and `load_wout_clebsch` passes `None`. Fix: `read_state` writes `T = knots(sp, deg)` into every block; consumers read `block["T"]`. `knots_at_data` (404, "for the wout refit") belongs in `vmec.py`, its only user.
3. **Equilibrium dispatch has three owners.** (high; the Clebsch mirror now, the nfp fast path later) `geometry.geometry_nfp` (330-340), `gvec.read_equilibrium` (310-320), `gvec.load_clebsch` (391-397) each restate ".dat -> gvec, .nc -> vmec, else raise" with the same message; `build_sequence` adds an `isfile` variant. `load_state_clebsch` / `load_wout_clebsch` are line-for-line mirrors differing only in `profile_spline`. Fix: one reader returning a state dict carrying its profile splines; `load_clebsch(st)` one function; `geometry_nfp` reads the state's `nfp`.
4. **`gvec._map_with_sign` (171) duplicates `mappings.stellarator_map` (125).** (high, now) Same formula; `stellarator_map` has no production caller and a `flip_zeta` knob flagged in the 2026-08-27 review. Keep `stellarator_map(R, Z, nfp, sign)` as the owner.
5. **Hand-rolled `evaluate_at_xq` via private `seq._form_comp_info`.** (high, now) `scripts/relax.py:296-320` (`make_force_normaliser`) and `mrx/relaxation.py:190-193` (`pressure_diagnostics`) rebuild what `seq.evaluate_at_quadrature(dofs, k, dirichlet)` does (same function calls it at 201/218). The normaliser is the residual scale in every trace: library, not script. Fix: `relaxation.force_normaliser(seq)` on the public method; add `seq.integrate_against(f_jk, k)` so the adjoint half is public (projectors.py also reaches for `_form_comp_info`).
6. **`scripts/relax.py:436-577`: the QoI sampler is library logic.** (medium) `force_probe` + `weak_pressure` + `pressure_diagnostics` + helicity + warm-start refresh is the per-state diagnostic every driver wants; `scripts/tutorials/li383_relaxation.py:116-121` re-derives `weak_p`. Fix: `relaxation.sample_diagnostics(state, seq, pw_guess)`. Separately `relaxation_loop` (792) and the driver's chunk loop are two loop owners with two trace schemas (`force_norm`/`energy` vs `F`/`E`). Sampler now; loop unification later.
7. **`scripts/poincare_relax.py`: the shared-scale rule computed twice, the p-scale constant duplicated.** (high, now) The "one iota, one p range across fields and planes" union appears in the `--from-npz` branch (230-249) and the trace branch (323-343); both hard-code `100.0`, while `render_section` owns `pressure_scale=100.0` (`plotting.py:412`). `SectionLimits.coerce` (`plotstyle.py:107-118`) exists to accept "the legacy dict or a bare `iota_lim`" because the script passes both. Titles built twice (262/369). Fix: `plotting.shared_limits(sections, pressure_scale) -> SectionLimits`; `render_section(limits: SectionLimits)` only; delete `coerce` and `iota_lim`.
8. **`mrx/plotting.py:30-35` re-export shim with no caller.** (medium, now) Nobody imports `FIELD_CMAP`/`SectionLimits` from `plotting`. `plot_twin_axis` (255-281) mirrors `LEFT`/`RIGHT` into eight kwargs and takes `*_plot_kwargs`; keep the dict path.
9. **`mrx/geometry.py:178-196,251` recompute `SplineMap.raw`.** (medium-high, now) `SplineMap.__init__` already stores `raw = E^T c`; `spline_map_F_DF_at_quad(coefficients, extraction_T, seq)` rebuilds it, which is why `from_spline_map` guards on `extraction_T is None`. Pass the map, read `.raw`, drop `extraction_T`. Also `compute_geometry_terms` (72) has no callers; `rotating_ellipse_map` (`mappings.py:62-69`) keeps `if nfp > 0` branches after raising on `nfp <= 0`.
10. **`build_sequence`/`geometry_nfp` sit in the module everything imports from** (`geometry.py:300-391`). (medium, later) Three `noqa: PLC0415` lazy imports, a `print` in the library, hard-coded `betti_numbers`. Move to a top-of-stack `mrx/equilibrium.py`; return `info`, let the script print.
11. **Precision selection repeated in seven scripts** with two idioms (`os.environ[...] =` vs `setdefault`, differing error text). (medium, later) `precision.py` is the one owner inside the package; the scripts want one `set_precision(cli)` helper before `import mrx`. `MAP_BATCH_SIZE_INNER` (`__init__.py:18`) is a mutable global two scripts populate from `MRX_MAP_BATCH_SIZE_INNER` themselves.
12. **`mrx/poincare.py:320-433`: unwrapped angle and least-squares slope written three times** (`rotational_transform`, `_iota_convergence`, `_iota_window_scatter`). (low-medium, later) One `_winding` + `_slope`.
13. **Small duplicates.** (now) `initial_conditions.project_reference_two_form:573-577` re-implements the k = 2 `Pushforward`; `initial_conditions.divergence_norm` (585) = `relaxation.compute_divergence_norm` (52); `TimeStepper.update_field` (541) has no caller; `State.A` is a driver warm start carried through the jitted step untouched.
14. **`mrx/spline_bases.py`: two B-spline evaluators.** (medium, later; verify tables bit-match first) The dense `_p_spline` recursion (`evaluate`) feeds every jitted table; the de Boor `_nonzero_bsplines` feeds point evaluation.

Clean: `differential_forms.py`, `quadrature.py`, `precision.py`, `vmec.py` (its only issue inherited from items 2/3).
## Scripts and tests: reuse

1. **Hand-rolled pushforwards.** (high) `scripts/poincare_relax.py:281-298` `physical_pressure` evaluates a 0-form directly and a 3-form as `val / det(map_jacobian_at(...))`: exactly `Pushforward(f, seq.map, k)` (`mrx/differential_forms.py:300-333`); `scripts/tutorials/qa_vacuum_field.py:77` shows the intended call. `scripts/vacuum_convergence.py:128-133` `pushforward_on_grid.one` retypes the k = 2 / k = 1 branches of the same class.
2. **`scripts/map_projection_study.py` re-implements `mrx.gvec` internals.** (high) `:37-42` `_sign_of` is the handedness loop of `build_gvec_map` (`mrx/gvec.py:343-352`) verbatim; `:81` the `.nc`/`.dat` dispatch is `read_equilibrium` (`gvec.py:310`); `:89-90` `StateField` construction duplicates `gvec.py:337-338`. The "l2" route is `build_gvec_map` itself.
3. **`scripts/tutorials/qa_vacuum_convergence.py` duplicates `scripts/analytic_vacuum.py`.** (high) `b_star` (`:63-70`) = `b_star_phys("coil")` (`analytic_vacuum.py:93-99`); `bstar_norm_sq` (`:73-76`) = `analytic_norm_sq` (`:111-124`); Route A/C (`:86-109`) = `run_rung` (`:164-218`) minus diagnostics. Same-directory imports are already the pattern (`qa_vacuum_sweep.py:12`).
4. **`relax.json` readers and summary rows.** (medium-high) Four independent loaders: `li383_pub_figures.py:28-35`, `compare_relaxations.py:37-58`, `li383_summary.py:51-68`, `plot_relaxation.py:98-99`. Relative helicity drift at `li383_pub_figures.py:62`, `li383_summary.py:67`, `compare_relaxations.py:128`, `relax.py:665`. `li383_summary.py`'s row is `li383_pub_figures.row_common`/`reader_rows` (`:70-131`) with a second formatter.
5. **B.h5 layout written four times, read twice.** (medium) Writers: `relax.py:529-543` `write_field`, `tutorials/li383_relaxation.py:143-153`, `li383_island_seed.py:162-173`, `li383_resistive.py:166-177`. Readers: `poincare_relax.py:181-218`, `plot_relaxation.py:57-65`. The same three tutorials repeat the driver's precision guard (`relax.py:348`, x4), the seed-spec parse (`relax.py:393`, `li383_island_seed.py:94`, `li383_resistive.py:87`; `initial_conditions.py:68 parse_lambda` is the existing spec-parser pattern) and a `weak_p` closure (x4).
6. **Torus-figure boilerplate x7.** (medium) `zetas`, `grids_pol`, `grid_surface`, `plot_torus(..., cstride=8, gridlinewidth=0.3, elev=25, azim=40)`, savefig/close: `plot_relaxation.py:70-96`, `li383_relaxation.py:129-140`, `li383_island_seed.py:148-159`, `li383_resistive.py:152-163`, `qa_geometry.py:100-116`, `qa_poisson.py:70-86`, `qa_vacuum_field.py:88-104`. The primitives (`mrx/plotting.py:38,109,185`) exist; a one-call wrapper does not.
7. **`MRX_MAP_BATCH_SIZE_INNER` env override x3.** (medium-high) `analytic_vacuum.py:235-237`, `vacuum_convergence.py:185-187` and `:417-419` each read the env var and poke `mrx.MAP_BATCH_SIZE_INNER`; `mrx/__init__.py:18` sets the constant but never reads the env; `precision.py:20` is the precedent.
8. **tanh eta schedule x2.** (medium) `tutorials/li383_resistive.py:97-99` re-derives `relax.py:207-208`; neither lives in `mrx.relaxation`, whose loops both take the schedule.
9. **Trace smoothing: two conventions.** (medium) `li383_pub_figures.py:181-210` `blocked`/`plot_trace` is the house rule; `compare_relaxations.py:31-34` `running_mean` smooths the same traces differently; `plot_relaxation.py:100` plots raw F. `blocked` belongs in `mrx/plotting.py`.
10. **Scalar `relative_l2_error` x3.** (medium) Identical in `tutorials/polar_poisson.py:39-44`, `mixed_polar_poisson.py:41-46`, `toroid_poisson.py:46-51`; `test/manufactured.py:212-238` covers k = 0 and 3 and is already imported by `poisson_study.py:89`.
11. **Small repeats.** (low) log-log slope fit x4 (`vacuum_convergence.py:463-473`, `analytic_vacuum_tex.py:50-55`, `analytic_vacuum.py:268-270`, `qa_vacuum_convergence.py:119`); `_log` x3; `TYPES`/`BETTI` constants in `test/conftest.py:30-32`, `poisson_study.py:95-96`, `map_projection_study.py:34` (all inside `build_sequence`, `geometry.py:395-397`); `vacuum_convergence.py:154-172` `trace_iota` calls `section_figure` and discards the figure where the headless `logical_field`/`seed_from_axis`/`trace_and_classify` are the calls it wants; `poincare_relax.py:342-359,376` builds the loose `limits` dict that `SectionLimits.coerce` labels "legacy".
12. **slurm.** (medium) `scripts/li383_sweep.sh:23-47` duplicates `li383_pub.sh:39-53,214-222`, and its `COMMON` passes `--method cg --save-every 100`, neither accepted by `relax.py`: a stale copy, delete. `slurm/regen_poincare.sh:58-72` re-assembles the venv/PYTHONPATH/sbatch wrap that `slurm/run.sh:55-71` provides.

Clean: `quad_order_equivalence.py`, `qa_vacuum_sweep.py`, `analytic_vacuum_tex.py` (bar `fit_order`), `poisson_study.py` (bar `_log`/constants), `li383_pub_figures.py` (home of `blocked`/`island_width`), `test/*`, `slurm/run.sh`, `slurm/waitjob.sh`.
## Scripts and tests: simplification

1. **`scripts/li383_sweep.sh`: dead launcher.** (high) Passes `--method cg --save-every 100`, neither parses; `submit/arm/sections/movie` verbatim copies of `li383_pub.sh`. Delete (77 lines that fail on launch).
2. **`scripts/quad_order_equivalence.py`: one-off migration check** for the r-major quad grid that landed 2026-08-28; nothing references it. Delete (147 lines). (high)
3. **`scripts/poincare_relax.py:228-277 vs 325-383`: the `--from-npz` branch re-implements the render path.** (high) Gauge, global p-limits, title/subtitle and the `render_section` call written twice with different key spellings; the npz path already lacks `traced in` in the subtitle. Simpler: trace -> build the `sections` dict -> save npz -> ONE `render(sections)` for both paths. `sections[f"{tag}_a_eff"]`/`_xlabel` (390) stored but recomputed on the npz path; `which = fields` (222) an alias.
4. **`scripts/li383_pub_figures.py`: arm lists and figures copied.** (high) (a) the 19-name list at 803-823 repeated as `reader` (1048-1058), `seeded` (1063-1074) and at 1004-1015: four copies; (b) `hsweep_figure` (260-338) and `psweep_figure` (601-659) are the same 60 lines with `ns[0]` swapped for `p`, while `hsweep_rows` already parametrises with `key=`; (c) `island_width_ic` (1108) is `island_width(..., tag="ic")` defined after `main`; `chaotic()`/`sections()` reload the npz per call (eta_islands loads it three times per arm); (d) `eta_traces` 477-484 filters `*_floor1e-5` arms, leftovers of the first eta launch. ~150 lines.
5. **`slurm/regen_poincare.sh`: second sbatch wrapper with site values in the repo.** (high) Hardcodes account, partition, excluded node, TEXBIN (37-43), contradicting `run.sh`'s rule; carries a `REPO`/`CODE` split for the since-merged plotter branch (20-27); default `ROOTS` includes the dead `outputs/li383_sweep`.
6. **`scripts/relax.py`: options nothing runs.** (medium, Tobias decides which experiments are closed) `--eta-schedule constant|linear`; `--method gradient`, `--dt-mode fixed`/`--dt0`, `--ic dzeta` (August research notes only); `--presmooth*` (247-252, 410-430) kept "for grid-only sources" though the h5 route was deleted 2026-08-28; `--stepper bonly` imports `mrx.experimental` into the production driver for the concluded 5f experiment. ~70 lines, 8 flags. Minor (high): `--seed/--seed-eps` absent from the docstring flag list; `params = {k: v for ...}` is `dict(vars(cli))`; `sample_qoi` both returns `(h, JoverB, diag)` and stores them in `latest`.
7. **`scripts/li383_pub.sh`.** (medium) `deep()` (82-89) duplicates three `seeded` arms: `COMMON` has carried `--floor-tol 1e-4` since 2026-09-02, so the `_f4` arms are identical reruns. The `case` repeats `SUB=..; PUB=..; LEDGER=..; mkdir` four times. `bonly/pulse/reconnect` export `PYTHONPATH=$WT` and pass `$WT/scripts/relax.py` while `arm()` uses relative paths: a no-op distinction with `MRX_ROOT` defaulting to `$WT`.
8. **`scripts/vacuum_convergence.py`.** (medium) `regrid_rung` (402-452) copies the grid-push block of `run_rung` (330-358); `import time as _t` (413) shadows the module import; `slopes["D_by_p_prefloor"] = slopes["D_by_p_preelbow"]  # alias for the note` (611) read by `analytic_vacuum_tex.py:137`: rename one side; `fmt` defined twice inside loops; `E_map_max` stored, never plotted. `qa_vacuum_sweep.py` builds argv strings to call `vc.parse_args`.
9. **Three li383 tutorials: ~80 lines copied** (precision guard, `weak_p`, torus plot, the `B.h5` writer with fabricated attrs); `polar/mixed_polar/toroid_poisson.py` repeat `disk_map`, `relative_l2_error`, `main`. (medium; tutorials are meant standalone, judgement call; the h5 writer belongs next to `write_field`.)
10. **`test/test_spline_bases.py:1-2, 12-16, 49-56, 104; test/test_quadrature.py:36-42`: empty section headers and docstrings naming Bernstein/tensor tests deleted in the lean-suite cut.** (high)
11. **`test/test_preconditioner_kind_dispatch.py`**: 262 lines, ~120 of narrative docstring about the 2026-08-25 drift; `KIND_TAKING_CALLS` (71-75) lists `apply_laplacian_preconditioner` twice. (low-medium)
12. **`scripts/li383_summary.py`**: summary for the dead sweep (default `outputs/li383_sweep`), `.get` fallbacks for older json layouts (58, 61); `li383_pub_figures.py` already emits table rows. Delete or fold. (medium)
13. **`scripts/compare_relaxations.py:89-112`**: `--hf` is a 2-panel subset of the 9-panel loop with its own x-axis selection; `load()` returns `t_of`, unused. (low)
14. **`scripts/poisson_study.py`**: `saddle_preconditioner='auto'` threaded where it is `'auto'` either way (160, 205); `_log` flushes twice; `--quad-order` no user; `BETTI`/`types` duplicate `conftest.py`; uses `compute_nullspaces_iterative` while claiming "solves what production solves". (low-medium)
15. **`slurm/README.md:10` cites `test/test_solvers.py`, which no longer exists.** (high)

Clean: `slurm/run.sh`, `slurm/waitjob.sh`, `plot_relaxation.py`, `analytic_vacuum_tex.py` (bar the alias), `map_projection_study.py`, the qa tutorials, `conftest.py`, `test_assembly`, `test_complex`, `test_poisson`, `test_precision`, `test_readers`, `test_relaxation`, `manufactured.py`, `synthetic_gvec.py`.
## Scripts and tests: efficiency

Measured inputs: `[setup] operators+nullspaces` 90 s at (12,24,12) p = 3, 152 s at (16,32,32) p = 2, 235 s at (32,64,64) p = 2; one field trace in `poincare_relax.py` 24 s at (16,32,32), the same for ic and final (nothing cached between fields).

1. **Default gap sweeps: 10 shifted saddle solves per build, for a print.** (high mechanism, medium magnitude) `compute_nullspaces(gap_sweeps=5)` runs `estimate_spectral_gap` for the k = 1 free and k = 2 Dirichlet forms whenever `verbose` (default), each a distinct shifted solve (own compile) plus 5 sweeps, ~17 s on W7-X (12,24,12) p = 3 by its own docstring. Nobody reads `lambda_1`: `test/conftest.py:47, :85` (both session fixtures; `test_harmonic_forms` checks the Rayleigh quotient itself: pass `gap_sweeps=0`, saves 2 compiles + 10 solves per fixture, 30-60 s of the 4-min suite), `scripts/relax.py:364`, `tutorials/li383_relaxation.py:259`, `li383_island_seed.py:91`, `li383_resistive.py:421`, `qa_vacuum_field.py:1088`, `qa_vacuum_convergence.py:967` (per rung).
2. **Gap estimate computed twice per form.** (high) `vacuum_convergence.py:210` (default sweeps inside `compute_nullspaces`) then `:215` and `:237` call `estimate_spectral_gap` again for the same forms; `analytic_vacuum.py:251` passes `gap_sweeps=cli.gap_sweeps` and `:184` repeats it for k = 1. Its own help says these sweeps are "most of Route A's time at n >= 32". Fix: `gap_sweeps=0` in `compute_nullspaces`, keep the explicit calls. Minutes on the large rungs.
3. **Preconditioners built by scripts that never solve.** (high) `build_sequence` (`geometry.py:391`) always runs `build_preconditioners()` for all 8 (k, BC) pairs. Callers needing only map and bases: `poincare_relax.py:279` (sections job ~150 s setup at (16,32,32) against 24 s per traced field: setup is over two thirds of every `sections`/`movie` job), `plot_relaxation.py:453`, `vacuum_convergence.py:429` (`--regrid`, whose docstring claims it "rebuilds only the geometry"), `tutorials/qa_geometry.py:760`; `qa_poisson.py:838` needs only k = 0 Dirichlet (as `polar_poisson.py` does with `ks=(0,)`). Cheaper: `DeRhamSequence` + `build_gvec_map` + `set_map`, or `build_preconditioners(ks=..., dirichlets=...)`. 1-4 min per job.
4. **Tracer recompiles per field.** (medium; library-side fix, driver pays) `poincare.py:249-273`: `trace` builds `ODETerm(cross_section_rhs(field))` around the fresh closure `logical_field` returns per DoF vector, so diffrax's cache misses on every field: 4 compiles per field (axis probe, main trace, drift at h and h/2). `--fields reconnect` is 10 fields, `snapshots` dozens; identical 24 s for ic and final confirms no reuse. Pass the DoF array as a traced argument. Est. 5-15 s per field, minutes per movie.
5. **relax.py: an M_0 solve on every step for the residual normaliser.** (medium) `relax.py:498` puts `normaliser` (0-form load, `apply_inverse_mass_matrix`, gradient, norm) inside the compiled step via `extra=`; the quantity drifts at the percent level and is consumed as a chunk mean. Once per chunk on the host. Est. 1-3% of a 0.57 s step, 2-5 min over an 18000-step ladder.
6. **poisson_study.py: DF materialised at all quadrature points 8x per n.** (medium) `:429` loads every case with `frame='phys'`; `projectors.py:316/329` recompute `map_jacobian_at(seq.map, seq.quad.x)` per call; at n = 64 p = 3 ~4.6 GB eight times for a consistency check that reports ~0.
7. **vacuum_convergence.py: recomputed Jacobian and per-call jit closures.** (medium-high) `:266-277` materialises DF at all quadrature points and inverts it on the host, though the stored metric suffices (`metric_jkl`, `metric_inv_jkl`, `jacobian_j`); repeats the most expensive step of `set_map` (the 10 GiB gather the file warns about at (33,64,32) p = 4) plus a host inverse of millions of 3x3. `:128-135`: a fresh `@jax.jit` closure per call, 4 compiles per rung.
8. **li383_pub_figures.py: repeated file loads.** (low) eta arms `load()`ed up to 4x; `sections()` reopens the 6-19 MB npz ~10x per arm. `lru_cache` both. 5-15 s of a 30 s script.

Clean: `li383_pub.sh`, `compare_relaxations.py`, `map_projection_study.py`, the poisson tutorials, all tests other than the conftest sweeps.
## Scripts and tests: altitude

1. **`scripts/relax.py:296-320`: the force-residual normaliser is library code in the driver.** (high, now) Re-implements `seq.magnitude_squared_load` (`derham_sequence.py:973`) through the private `_form_comp_info`, then adds `M0^-1`/grad/norm. Recommend `mrx.relaxation.force_scale(seq, B)`; driver, `chunk_runner`'s `extra` probe and tests call one thing.
2. **Three tutorials are ~170-line copies of the driver's mechanics.** (high, now) `tutorials/li383_{relaxation,island_seed,resistive}.py` repeat the MRX_DTYPE preamble, `build_sequence` + nullspaces, IC, the `TimeStepper` recipe, `weak_p`, the torus-plot block, and a hand-written `B.h5` writer that forges attributes (`maxiter=10_000`, `nfp=""`, `method="lbfgs"`) so `poincare_relax.py:213-279` can parse them: the reader's schema leaked into four writers. Recommend `mrx.relaxation.write_state/read_state` + `sequence_from_state(attrs)` used by `poincare_relax.py`, `plot_relaxation.py:458-466`, `vacuum_convergence.py:361-373`; `weak_pressure_of(B, seq)`; `mrx.plotting.torus_grids(map, cuts, n)` (six lines in seven places). Tutorials shrink to ~40 lines.
3. **The `mu = 0.064 / n_r^2` rule has six owners.** (high, now) Tutorials (`li383_relaxation.py:89`, `island_seed.py:268`, `resistive.py:441`), `test/test_relaxation.py:32`, `docs/source/tutorials.md:154`, and `li383_pub.sh:36-37,98-102,165` carrying five hand-computed absolute scales. Recommend the stepper/driver default the scale to `0.064 h^2` when the order is set and no scale given; launchers pass only the order. Same for the resistive ladder `eta K dt = 2e-5` at `li383_pub.sh:130`.
4. **`scripts/li383_sweep.sh` is dead.** (high, now) Passes flags `relax.py` no longer parses; `submit`/`arm` verbatim duplicates of `li383_pub.sh:39-53`. Delete.
5. **Two eta-schedule implementations.** (high, now) `relax.py:186-211` (jnp, per step) and `li383_resistive.py` (np, per block midpoint, subtly different sampling), plus the docs copy. Move `eta_schedule` into `mrx.relaxation` next to `chunk_runner`.
6. **`slurm/regen_poincare.sh` is a second sbatch wrapper.** (medium, now) Hardcodes partition/account/excluded node/TEXBIN where `run.sh` reads `site.env`; `li383_pub.sh:212` hardcodes the TeX path a third time. Regen should loop over `run.sh` with `EXTRA_ENV`; TEXBIN belongs in `site.env`.
7. **Sequence setup has four entry paths and three nullspace entry points.** (medium; the flag now, the fixture later) `build_sequence`, `set_map_and_preconditioners` (three poisson tutorials), `set_spline_map(greville_interpolate_map)` (`test/conftest.py:83`), `DeRhamSequence`+`set_map`+`build_preconditioners` (`poisson_study.py:210-224`). Nullspaces: module `compute_nullspaces(seq, ops)` pasted after every `build_sequence` (8 sites), `seq.compute_nullspaces(BETTI)` (`conftest.py:85`), `init_nullspaces` (`mixed_polar_poisson.py:59`), `compute_nullspaces_iterative` (`poisson_study.py:243`). The `toroid` fixture calls itself "the production setup" but takes the sum-factorised spline-map path while production goes through `build_gvec_map -> set_map`. Recommend `build_sequence(..., nullspaces=True)` as the one owner, `poisson_study` on the production `compute_nullspaces`, the fixture built by `build_sequence`.
8. **Island width lives in a figure script.** (medium; width now) `li383_pub_figures.py:43-57` `island_width` reads `sections.npz` by string keys; `blocked()` at 181 is the ribbon convention while `compare_relaxations.py:31` uses a running mean and `li383_summary.py:362-381` re-derives s/step and drift a third way. Recommend `mrx.poincare.island_width(res, m, n, nfp)` computed in `poincare_relax.py` and stored in the npz; `mrx.plotting.block_stats`; one `load_run(dir)`.
9. **Seed spec parsing duplicated x3** (`relax.py:392-394`, `island_seed.py:254-255`, `resistive.py:427-428`); `parse_lambda` lives in `initial_conditions.py:68`. Add `parse_seed` beside it. (high, tiny)
10. **Vacuum routes duplicated between study and tutorial.** (medium, later) `analytic_vacuum.py:74-107,163-221` and `tutorials/qa_vacuum_convergence.py:183-229`; `vacuum_convergence.py:94-151` holds library-grade `common_grid`, `pushforward_on_grid`, `toroidal_flux`; `qa_vacuum_sweep.py:12` imports a sibling script. Recommend `mrx.vacuum`.
11. **`MRX_MAP_BATCH_SIZE_INNER` read in scripts.** (high, now) `mrx/__init__.py:18` should read it the way `precision.py` reads `MRX_DTYPE`.
12. **`relative_l2_error` x4** and `poisson_study.py:89` importing from `test/`, a script depending on the test tree. (medium, later) `seq.relative_l2_error(u_hat, exact, k, dirichlet)` owns the norm.
13. **Tests vs production path.** (medium, later) `test_relaxation.py` drives `relaxation_loop`, not the chunk/normaliser/qoi/reconnect/restart loop of `relax.py:496-642`: ~150 lines of untested mechanics because they sit in a script; items 1, 2, 5 make it testable within the lean-suite rule. `test_preconditioner_kind_dispatch.py:66` keeps its own `LIVE_KINDS` (still listing the deleted `jacobi`) rather than deriving it from `operators.py`.
14. **Minor.** `poincare_relax.py:295-297` hand-divides the 3-form by `det DF` where `Pushforward(..., 3)` exists; `poisson_study.py:68-80` parses argv at import.

Clean: `slurm/run.sh`, `slurm/waitjob.sh`, `analytic_vacuum_tex.py`, `quad_order_equivalence.py`, `map_projection_study.py` (a one-off measuring internals), `compare_relaxations.py` beyond 8, the qa tutorials beyond the grid block, the tests beyond 13.
