# Handoff 2026-09-09: the road to publication

Tobias, 2026-09-09: "the plan is: complete the figures and tables, then merge our own
work into one branch (new poincare plotter + Newton + anything else), then merge the
PR, then fix docs, then publish it all."

## 1. Complete the figures and tables

Convergence section (`outputs/newton_second_variation/convergence_section.tex`,
figures in `outputs/figures_2026-09/`):

- `newton_convergence_resolution0_wall_log.{pdf,png,pgf}` + `newton_convergence.json`
  + `newton_convergence_newton_steps.txt`, built by
  `outputs/newton_second_variation/newton_convergence_figure.py` (login node, reads
  the relax.json backups under `outputs/newton_second_variation/newton_relax/`).
  Regenerate once the last Newton arms land (below), then copy to the figures folder.
- The resolution table (L-BFGS(1) vs Newton from step 0 on the (n,2n,2n) meshes:
  steps, s/step, time, floor, steps/time to 1e-6/1e-7/1e-8, dH/H, rate fits in n):
  built by the tmp script `newton_table.py` (job dir
  `~/.claude/jobs/81d8eef9/tmp/`) from `newton_convergence.json`; move that script
  next to the figure script before the job dir is cleaned. Decisions taken: a
  single-step dip counts for the threshold crossings; times in s/min/h, decimal
  aligned with phantom digits, units in their own column; 32^3 rows out of the
  table (kept in the figure); the unfloored arm out of every fit.
- DONE 2026-09-09 03:00: the 120-minute continuations of the from-0 arms on
  (24,48,48), (32,32,32), (32,64,64) all rose above their earlier minima, so the
  floors stand: 1.95e-8 (12,24,24), 4.52e-9 (16,32,32), 2.50e-9 (24,48,48),
  4.00e-9 (32,32,32), 9.36e-9 (32,64,64) -- not monotone in the mesh, like the
  warm-start floors. Figure, json, steps file and `newton_table.tex` regenerated
  and copied to `outputs/figures_2026-09/` and `outputs/newton_second_variation/`.
  Rule since 2026-09-08: a floored arm gets no continuation; cancel if more than
  an hour is left (memory `no-continuing-floored-runs`). The section's
  `tab:convergence` still lacks the from-2500 and from-0 rows; Tobias is
  building the resolution table himself from `newton_table.tex`.
- QA vacuum agreement table, both VMEC references side by side
  (`outputs/figures_2026-09/vacuum_table.{tex,py}`, also under
  `outputs/qa_vacuum_highres/`): the six missing high-resolution cells, n = 37
  and 45 at p = 2, 3, 4, were run 2026-09-09 (jobs 18212796/97, 18215384,
  18212978; `scripts/qa_vacuum_sweep.py` from the main checkout, the two finest
  meshes need `MRX_MAP_BATCH_SIZE_INNER` set or the whole-grid map evaluation
  OOMs; at 32768 the batched evaluation is slow, ~2 h per rung at p = 4, a larger
  batch would do). Result: the high-resolution floor is 4.3 to 4.6e-5, flat over
  n = 29, 37, 45 and over p, as the low-resolution floor is flat at 8.4e-5, so
  both floors are the references' truncation. The mesh column is n x 2n x n as
  in the rest of the paper (the rungs' spline counts are n+p per direction).
- Poincare pages of the three from-0 Newton floor states, (16,32,32), (24,48,48),
  (32,64,64), five house planes, paper style, 3-form pressure labelled plain p:
  `outputs/figures_2026-09/newton_floors/<mesh>/` (archives `trace_floor.npz`
  next to the runs; `outputs/newton_second_variation/trace_floors.py`).
- `fig_energy_landscape_3d.{pdf,png,py}` (the isocline sheets as energy bowls, the
  ideal orbit as a line on the sheet) is final; `fig_energy_landscape.{pdf,png,py}`
  is the 2-D companion; caption drafted in this session.
- Ladder reconnection sections: rendered by the figs session at
  `outputs/reconnect_2026-09/ladder/h16_p2/poincare/` (paper style, `--inner-cells 0`).
- The relaxation videos (Tobias 2026-09-09: "finish the video. And make a second
  video that does exactly the same, but it starts with an initial condition that
  has the iota = 1/2 island seed" = `outputs/seeded_2026-09/s61_e1e-2`, the (6,1)
  seed at rho 0.544, eps 1e-2; "do not launch any crazy amount of jobs, just the
  two video jobs plus 5h of gpu time spare"). One GPU job per video runs
  `outputs/video/video_pipeline.py` (stages skip when done, so a job resumes):
  descent to residual 2e-4 with checkpoints every 5 steps, a 20-step restart with
  one 10% reconnection at step 10 (spends ~7.7%), descent to 1e-4, the Newton
  finish (step-5000 protocol, 40 min; the result is the lowest-mean chunk's
  checkpoint), the frame schedule by the visual clock sum(dt*resid) (144 frames
  per descent phase, rounded to checkpoints, `frames.json`), the figs-branch
  tracer at zeta = 0 on those checkpoints (120 lines, 200 periods,
  `<run>/trace_video.npz`) and the fly on the Newton result (480 planes per field
  period, `newton/fly.npz`). Roots: `outputs/video` (li383) and
  `outputs/video/s61`, jobs 18198252 and 18198255 (2026-09-09 02:12).
  Then on the login node `outputs/video/video_render.py --root ROOT`: the figs
  plotter (082222d) `--paper --inner-cells 0 --dpi 300` with every axis pinned
  across the four archives, `--window` (R/Z box), `--iota-lim`, `--p-lim`,
  `--rationals 15/38,3/7,6/13,3/6,6/11,3/5,9/14`, ~3.5 s per page, ~50 min per
  video, then ffmpeg (the venv's imageio-ffmpeg binary, libx264, 1920 px wide,
  24 fps) with the storyboard: A frames | 1 s pre-solve | cut | 0.5 s post-solve
  | C frames | 1 s Newton result | the fly | 1 s. Delivered to
  `/kfs3/scratch/tblickhan/mrx/outputs/video/<name>/relaxation_<name>.mp4`.
  DONE 2026-09-09: `li383` (14:24) and `s61` (17:44), 35 s each, both with
  every axis pinned and the 3-form pressure. Videos' relaxation stages: li383
  descent to 4665, solve at 4675 (7.7% of H), descent to 12685, Newton best
  12765 (1.5e-9); s61 descent to 5355, solve at 5365, descent to 13375, Newton
  best 13455 (1.7e-9).
  Lessons: the tracer's `--pressure weak` (the 0-form diagnostic) solves two
  systems per field BEFORE tracing any, silently (~2 min per field: five hours
  for 143 fields); the videos use `--pressure strong`, the checkpoint's Leray
  3-form, no solve (Tobias: the 0-form diagnostic is not needed, the 3-form
  pressure IS wanted). A snapshots archive with 480 planes renders as
  `frame_zeta<plane>_0000.png` per plane (the movie naming), not `fly_*`.
  Stage-1 stills of the first video (pre/post solve, paper style) are in
  `outputs/video/reconnect/poincare_paper/`.
  2026-09-10 additions (both videos recomposed, `video_render.py --skip-render`,
  ~5 min each): a strip under the three tiles with three running plots against
  the step, x fixed to the whole run from the first frame, y fixed, log scales:
  the force ratio ||JxB - grad p||^2 / (1/2 ||grad |B|^2||^2) (Tobias's label;
  the code divides by ||grad(B^2/2)||^2, i.e. the exact outside factor is 1/4),
  |dH|/H_0, dE/E_0; text-only legends, major grids only, each panel as wide as
  the tile above (the tiles' spines are read off the frame); a boxed
  "zeta = 0.xx" label in the section's top right corner (0.00 on the time
  frames, k/480 along the fly). The trace archives (all frames' field lines)
  are copied next to the mp4s: `<name>/{descent,reconnect,descent2}/trace_video.npz`,
  `<name>/newton/fly.npz`.
- Force balance per radial surface (Tobias 2026-09-10) for the seeded runs
  `outputs/seeded_2026-09/{s61,s51}_e1e-2`: `outputs/video/force_profile.py`
  (GPU: the Leray-projected force and JxB pushed forward on a logical grid,
  surface L2 norms per r, every checkpoint -> `<run>/force_profile.npz`) and
  `force_profile_plot.py` (login node: the end-of-run profile with JxB as the
  reference) -> `outputs/figures_2026-09/force_profile_<run>.{pdf,png}`. Finding:
  the residual is smallest RELATIVE to JxB at the islands (5e-4) and largest at
  the axis (1e-2) and the wall (5e-3); the along-line pressure variation seen in
  the Poincare colouring (6-20% of p_max on the 1/2 chain, 15-47% on the 3/5
  chain) is the line integral of the parallel residual over ~600 length units.
- The 3-form pressure on the logical plane (Tobias: "we would see if p is flat
  in islands"): `outputs/video/pressure_map.py` (GPU, p/detDF at zeta = 0 ->
  `<run>/pressure_map_<step>.npz`) and `pressure_map_plot.py` (full plane +
  island-band zoom with the resonant lines' crossings) ->
  `outputs/figures_2026-09/pressure_map_<run>.{pdf,png}`. Finding: p is NOT flat
  in the islands: p x100 falls 3.6 -> 3.1 across the 1/2 chain and 2.8 -> 1.25
  across the 3/5 chain, contours cutting through the lobes; the implied parallel
  gradient dp / connection length ~ 2e-5 lies below the 1e-4 residual floor, so
  the descent cannot feel it. Flattening p in islands needs a residual ~100x
  lower or resistivity.
- Vacuum-convergence jsons behind the paper's tables (`outputs/figures_2026-09/
  vacuum_vmec_convergence.json`, `vacuum_analytic_convergence.json`, built by
  `vacuum_jsons.py` there).
- Research note: `docs/research/newton_second_variation_2026-09-06.md`.

## Status 2026-09-10 (steps 2-4)

Done on `newton-second-variation` (the unified branch; push it to
`origin/static-dynamic-refactor` as a fast-forward once the suite passes):
- e176b05 merge of `origin/static-dynamic-refactor` (its two newer commits),
  feab04e merge of `figs-noaxes` (21 commits: the trace/plot split, `--window`,
  `--iota-lim`, `--p-lim`, `--rationals`, plain-p label). Both conflict-free.
  Suite 57/57 in all three configurations (jobs 18286008/12/13).
- 478f86e merge of `origin/tpu-support-v2` (PR #20, 28 commits): one conflict,
  `mrx/solvers.py` refine's pass loop, resolved by composing both fixes (our
  unit-norm residual scaling, 5887b72, and the PR's discard-a-worsening-pass
  rule). Suite round two (jobs 18286654/57/58): 59/59 in all three
  configurations (the PR's refine and shift-plan tests included).
  `origin/static-dynamic-refactor` fast-forwarded to the unified branch
  2026-09-10 ~10:00, which closes PR #20 as merged. Docs build warning-free
  (`sphinx-build` after `pip install -r docs/requirements.txt`; one stale
  duplicate `geometry` attribute entry removed from the sequence docstring,
  ba33fc6); `ruff check mrx scripts test` clean.
- Docs: the Newton flags added to relax.py's table in `docs/source/relaxation.md`
  (78863ac); no page names a deleted script any more (the figs merge brought the
  trace/plot names).
- By patch already in the integration branch: `midpoint-r16` (3 commits),
  `li383-followups`, `tutorial-cell-markers`, `poincare-plotter`.
- NOT merged, Tobias's decision: `worktree-simplify-sweep` (7 commits,
  2026-09-05, 64 files, -4344 lines: solver core, sequence, relaxation loop,
  Poincare/plotting, tutorials) was never merged and conflicts in seven files
  (`derham_sequence.py`, `metric_lumping_laplacian.py`, `relaxation.py`,
  `solvers.py`, `scripts/relax.py`, two docs pages) against the unified branch,
  whose plotting the figs branch has rewritten since. Recommendation: leave it
  out of this release. `worktree-main-cleanup` (the August clean-main candidate)
  and `worktree-relax-cli-prune-handoff` (two docs commits on old main) are
  superseded.

## Status 2026-09-10 ~12:00 (simplify sweep merged in part)

Tobias: "merge what still applies and what is easy; ignore what the note says
about things rewritten since". Done as d9033b1 on `newton-second-variation`:
`git merge --no-commit worktree-simplify-sweep`, 16 conflicted files taken from
ours (`derham_sequence.py`, `mass.py`, `metric_lumping_laplacian.py`,
`plotting.py`, `poincare.py`, `relaxation.py`, `solvers.py`, `scripts/relax.py`,
`scripts/plot_relaxation.py`, the tutorials, `docs/source/relaxation.md`,
`tutorials.md`, `concepts/mass.md`, `precision.md`, `concepts/relaxation.md`;
`scripts/poincare_relax.py` stays deleted), plus three auto-merged modules
put back to ours because the kept modules call what the sweep removed:
`operators.py` (`extraction`, `_require_bundle`, the assembler keywords
`dirichlet_variants`/`bands`/`overlap`/`bc_scale`), `projectors.py`
(`load(bc=)`), `extraction_operators.py` (`bc_extraction_op`). Taken: the
`mrx/__init__` star imports, the GVEC reader signatures (no `sp`, no `sign`),
`nullspace`, `differential_forms`, `quadrature`, `precision`, `preconditioners`,
`spline_bases`, `mappings`, `geometry`, `vmec`, `initial_conditions`,
`plotstyle`, the tests, and the docs (PRODUCTION.md, concepts.md,
slurm/README.md gone, API index trimmed, eight concept pages rewritten).
`scripts/map_projection_study.py` adapted to the reader signatures. The sweep
note `docs/research/simplify_sweep_2026-09-05.md` now opens with what went in
and what did not. Checks: removed-name scan (every name the sweep deletes or
re-signs, private included, against every caller), ruff clean, all modules
import, docs build 0 warnings, suite 54/54 in the three configurations (jobs
18292544/45/46; 59 -> 54 is the five parametrised `spectral_quad` cases the
sweep deleted with the function). `origin/static-dynamic-refactor`
fast-forwarded to d9033b1+.

## Status 2026-09-10 ~15:15 (Newton tutorial)

Tobias: "I want the tutorial to also explain newton, maybe before the
reconnections (and then reconnection and island seeding use newton)", and the
budget: "show the api and QUICKLY give some REASONABLE results ... 5-10 newton
steps", "run this on a laptop even in say sub 1h". Done on
`newton-second-variation`:
- `scripts/tutorials/4_li383_newton.py` (new): warm-starts from Tutorial 3's
  floor (or runs that descent itself), continues the smoothed descent (200
  steps) and Newton (10 steps, the study protocol: Laplacian atom, tol 0.1,
  300 iterations, dt cap 1) from the same state, draws `||F||` against the
  step and the wall time for both, archives the Newton run. Measured at
  (10,16,16) p=2: descent 1.5e-3 -> lowest 1.2e-3 in 200 steps (57 s), Newton
  1.5e-3 -> 3.3e-4 at step 6 then turning (4.4e-4 at 10, 103 s), dH/H -2.6e-4.
  Job 6 min 41 s on an H100 in all.
- `5_li383_island_seed.py` (was 4): after the two initial-field sections it
  relaxes the seeded field ideally, 200 descent + 5 Newton steps, and sections
  it again: the (6,1) chain is still there, residual 2.1e-3 -> 3.1e-4,
  dH/H -4e-5. 12.5 min, three traces.
- `6_li383_resistive.py` (was 5): warm-starts from Tutorial 4's Newton floor
  (then 3), one resistive step, 5 Newton steps instead of the 500-step ideal
  tail: 1.5e-3 -> 1.1e-4. 7 min.
- The mesh stays (10,16,16) p=2: at (16,32,32) the chain is a GPU-hour
  (Newton 17 s/step), which Tobias rejected. On ten radial cells the Newton
  direction is good for a handful of steps and then leaks (measured today: 60
  steps from Tutorial 3's floor turn at step 6 and lose 0.6% helicity; the
  seeded field goes chaotic after 40), which the tutorial and the docs say,
  with the study's section 10e as the reference. Five to ten steps is the
  budget.
- `docs/source/tutorials.md`: six sections, the Newton section with the
  stepper call and the study's numbers; `docs/source/relaxation.md`'s Newton
  row points at the tutorial. Docs build 0 warnings, ruff clean; the library
  is untouched, no suite rerun.
- Tobias: "provide the users with checkpointed states for all these so they
  can run them in any order." `data/tutorials/<name>/` (un-ignored in
  `.gitignore`): a params-only `relax.json` and the last checkpoint of each
  li383 tutorial's run at the shipped defaults, 1.4 MB in all. Tutorials 4 and
  6 look in `outputs/tutorials/` first and fall through to `data/tutorials/`
  (tested with the outputs folder set aside: both warm-start from the shipped
  state and reproduce the runs). `scripts/poincare_trace.py --run
  data/tutorials/li383_newton --out <elsewhere>` traces a shipped state; the
  tracer now creates the archive's directory.

## Status 2026-09-10 ~17:00 (knots, precision "mixed", default mesh)

Tobias: "this radial refinement thing is not well done in the code, we should
just be able to pass knot vectors to r, theta, and zeta as lists of knots ...
and call it knots"; "default precision should be mixed"; "change the default
to 16 32 32 on the mesh ... in the code".
- `mrx.geometry`: `parse_r_refine` and `radial_knots` (the window spec) are
  gone; `knot_vector(breakpoints, p, periodic)` pads a breakpoint list to the
  full knot vector (clamped: ends repeated `p` times; periodic: the `p` cells
  either side continued by the period, the structure `SplineBasis` builds),
  `parse_knots` reads a comma list. `build_sequence(..., knots=(r, theta,
  zeta))` takes breakpoints per axis (`None` = uniform) and derives that
  axis's `n` (cells + p clamped, cells periodic). The sequence constructor
  keeps its full-knot-vector `knots=` and now checks the periodic count too.
- Angular breakpoints must be uniform: the map's series projection
  (`mrx.gvec`, the circulant collocation/mass row) raises on a non-uniform
  periodic basis (measured: the three-axis smoke run failed there), so
  `build_sequence` rejects non-uniform angular breakpoints with that reason.
  Nothing else in the sequence, the solves or the atoms assumes uniform
  angles (no Fourier symbol anywhere but that projection). Lifting it is
  local: per mode, the moment against the non-uniform basis by quadrature
  and one 1-D mass solve instead of the division by the symbol
  (`series_tensor_coefficients`); not done, and the atoms are untested on
  non-uniform angles.
- `scripts/relax.py`: `--knots-r/-theta/-zeta LIST` replace `--r-refine`;
  `params["knots"]` (three lists or null) and `params["ns"]` (derived) are
  what `poincare_trace.py` and `plot_relaxation.py` read back (`r_refine` is
  gone from the run files; the four shipped `data/tutorials` files updated).
  `--precision {mixed,float32,float64} [mixed]` exports both `MRX_DTYPE` and
  `MRX_RESIDUAL_DTYPE` (mixed = float32/float64, the old default under its
  right name; float32 = plain float32, the TPU configuration); the `[env]`
  line names the mode and both dtypes. `--ns` default `16,32,32`.
  `plot_mesh.py`: `ns|breakpoints` per mesh.
- Checks: ruff clean, byte-compile, docs 0 warnings; radial-knots smoke
  (li383, 11 cells -> n_r = 13, 20 steps, helicity -4e-5, job 18316990);
  float64 smoke (tol 1e-10, job 18316956); `plot_mesh.py` with breakpoints
  (job 18316977); the tracer on the knots run; suite 54/54 x3 (jobs
  18316944/46/47). Note for the runner: a `--meshes` spec with `;` or `|`
  must be single-quoted inside `ARGS` (the job shell re-parses it).

## Status 2026-09-10 ~17:30 (the production defaults)

Tobias: "the L-BFGS default should be m=1, the method default should be
newton", "smoothing should be on by default, gamma=1", "default velocity
should be potential route".
- `scripts/relax.py`: `--method {newton,lbfgs} [newton]` replaces
  `--newton {false,true}`; `--history M [1]` is the L-BFGS memory (Newton
  runs with history 0 by construction). The Newton defaults are the study's:
  `--newton-tol 0.1`, `--newton-maxiter 300`, `--newton-dt-cap 1` (were
  1e-3, 100, inf), also as `TimeStepper` and `newton_direction` defaults.
  `--velocity-smoothing-order [1]` (`TimeStepper` too).
  `--potential-velocity [true for the L-BFGS descent]`: `TimeStepper.
  potential_velocity=None` resolves to `True` unless `newton` or
  `auxiliary_B_field` is set (each has its own route); explicit `True` with
  either still raises. The run file records the resolved route.
- The study launch scripts `scripts/li383_pub.sh`, `scripts/midpoint_sweep.sh`
  carry `--method lbfgs` so they mean what they meant, and their two
  `--r-refine` specs became the breakpoint lists the old windows produced.
- Docs rows, tutorials 3 and 4 text and the options table follow. The
  tutorial chain is rerun at the new defaults (the descent steppers now take
  the potential route) and the shipped states regenerated.
- Checks: bare-default Newton smoke from the li383 field (5 steps at
  (10,16,16): tol 0.1 / 300 it / dt cap 1, smoothing on; job 18317725);
  L-BFGS smokes on both routes; suite in the three configurations.

## Status 2026-09-10 ~17:40 (the wall-clock budget is gone)

Tobias: "the wall-clock budget it seems outdated since we checkpoint now."
`relax()` has no `seconds` argument and no `"seconds"` stop any more: a
job's time limit is no stop, the checkpoint of every chunk restarts it with
`--restart`. `--seconds` is gone from `scripts/relax.py` and the docs rows;
the launch records `li383_pub.sh` (26 uses) and `midpoint_sweep.sh` (18)
had it stripped, their slurm limits are the budget. Suite in the three
configurations for it (jobs 18320976/77/78).

The tutorial chain at the new defaults (potential route in the descent
steppers): T3 500 steps 5.6e-2 -> 1.2e-3 (was 2.1e-3 by the Leray route),
T4 from it: descent 200 steps lowest 3.5e-4, Newton lowest 3.0e-4 at step 5;
T5 seeded: 2.0e-3 -> 2.6e-4 in 5 Newton steps; T6 follows. The shipped
states in `data/tutorials/` are regenerated from these runs.

## Status 2026-09-10 ~18:15 (round three of the defaults)

Tobias's table review: "the relaxation steps with newton default should be
100. The minres steps within newton I do not want to touch"; "update the
force residual to what I want it to be and make it squared"; "newton shift
... drop from code too"; "newton inner tol need not be its own thing";
smooth-first: the best default per `tab:velocity_choices`.
- `--steps [100 Newton, 3000 L-BFGS]`, `--chunk [20 Newton, 500 L-BFGS]`,
  resolved by method; MINRES budget stays 300.
- The recorded residual `resid` (trace, qoi, the floor test) is now the
  paper's squared normalised residual `||F||_M^2 / ||grad(B^2/2)||^2`, the
  old `resid` squared, the quantity the paper's tables list (the anchor's
  5.72e-8 in `tab:velocity_choices` is the old recorded residual squared,
  no half); `--floor-tol [1e-8]`; the tutorials' floors converted (1e-3 ->
  1e-6); the docs' calibration numbers converted. (An intermediate version
  of this afternoon carried a spurious factor 1/2; corrected 18:40.)
- `newton_shift` and `newton_inner_tol` are gone from `newton_direction`,
  `TimeStepper`, the driver and the docs; the Hessian's mass solves run at
  the sequence's tolerance. `smooth_first` defaults to `True` (the Leray
  route; the potential route is smooth-first by construction).
- Picard, measured from the midpoint-sweep traces (no new run): mean 2.0
  iterations per step in float32 (median 2, max 3), 3.5-4.0 in float64
  (median 4, max 5-6), at 1-5% more wall per step than the explicit arm.
- The shipped tutorial states stand: the MINRES budget is unchanged and the
  converted floors are never reached in the tutorials' budgets.
- Tobias: "we can do 1D mass solves at setup, zero reason not to". The map's
  series projection no longer needs uniform angular bases:
  `mrx.gvec._angular_coefficients` computes, per mode frequency, the
  moments against the periodic basis by Gauss quadrature on its cells (the
  point count follows the mode's phase across the widest cell) and one
  N x N mass solve shared by all modes, at setup; the product mode's tensor
  coefficients are the real (cosine series) or imaginary (sine) part of the
  outer product of the theta and zeta vectors. On uniform bases it
  reproduces the circulant closed form it replaces to 3e-15 (li383 R, Z,
  lambda at (16,32,32) p=2; `compare_projection.py` in the job scratch), and
  a periodic basis on non-uniform knots passes partition of unity to
  round-off with the projection error scaling with the largest cell.
  `build_sequence` accepts non-uniform angular breakpoints again; the
  three-axis knots smoke is the end-to-end check.
- Tobias: "since we now have the smooth-first numbers, we can hardcode the
  better of the two". `smooth_first` is gone: the force is smoothed before
  the L-BFGS combination on both routes (the smooth-last branch deleted).

## 2. Merge our own work into one branch

Worktrees at the time of writing (`git worktree list`): `static-dynamic-refactor`
(main checkout, the integration branch per the 2026-08 endgame), `figs-noaxes`
(new `scripts/poincare_trace.py` + `scripts/poincare_plot.py`, `--window`),
`newton-second-variation` (this: `mrx/hessian.py` Newton direction, `relax.py`
`--newton*` options, `refine` tol fix, the study), `li383-followups`
(vmec-axis-guard), `midpoint-r16`, `poincare-plotter`, `tutorial-cell-markers`,
`worktree-main-cleanup`, `worktree-relax-cli-prune-handoff`, `worktree-simplify-sweep`.
Check which of the last six are already in `static-dynamic-refactor` before merging;
the peer sessions know their own. Suite gate for every merge: `bash slurm/suite.sh`
(three configurations, 57 tests each on this branch).

## 3. Merge the PR

PR #20 (`tpu-support-v2`, open): the shift-plan mass kernel (no gather/segment_sum
index tensors), `tpu/` as the `slurm/` analogue, the relaxation-loop re-jit fix.
An earlier merge of `tpu-support` into vmec-axis-guard was reverted on 2026-09-03
(59e3970, no reason recorded). Review notes: its 17 s/step on TPU was compile; the
kernel change was bit-identical on the trajectory. Merge after step 2 so it lands
on the unified branch; re-run the suite in the three configurations.

## 4. Fix docs

One Sphinx tree under `docs/source`; tutorials in `scripts/tutorials`; the
production scripts are the tutorials, `relax.py`, and the poincare pair. Remove
mentions of deleted scripts (`poincare_relax.py`, `poincare_vacuum_qa.py`), add the
Newton options and the trace/plot split.

## 5. Publish

Per the 2026-08 endgame: a fresh single-commit `main` from the unified branch.
`slurm/site.env` never goes out. Tobias reviews everything on GitHub; nothing is
posted there from the sessions except pushed commits and draft PRs.
