# Quick test run with minimal settings
# Usage: python scripts/config_scripts/relax_from_nfs.py +experiment=test
# @package _global_

defaults:
  - /resolution: low

relaxation:
  num_iters_inner: 10
  num_iters_outer: 2

fieldline:
  enabled: true
  every: 1
  T: 10000.0
  n_traj: 32
