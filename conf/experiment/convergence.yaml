# Convergence study: sweep over resolutions
# Usage: python scripts/config_scripts/relax_from_nfs.py +experiment=convergence
# @package _global_

defaults:
  - /resolution: medium

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      resolution: low,medium,high
